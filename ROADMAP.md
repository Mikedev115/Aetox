# Roadmap — 1.0.0 (Windows) แล้วต่อด้วยสามแพลตฟอร์ม · อัปเดต 2026-08-25

> **จุดยืน (เจ้าของ, 9 ส.ค. 2026):** **โฟกัสที่ Windows เป็นหลัก** — Windows คือแพลตฟอร์ม
> อ้างอิง ทุกฟีเจอร์เกิดที่นี่ก่อนเสมอ และสาย **0.9.x ปล่อยถี่ ๆ บน Windows** โดยไม่ต้องรอ
> แพลตฟอร์มอื่น
>
> **macOS กับ Linux คือการตามให้ทัน ไม่ใช่สายพัฒนาคู่ขนาน** — งานพอร์ตไม่มีสิทธิ์ถ่วง
> สายฟีเจอร์ และ **1.0.0 ไม่ใช่วันที่ แต่เป็นเกณฑ์**
>
> **แก้เกณฑ์ 15 ส.ค. 2026 (เจ้าของเคาะ):** ครึ่งหลังของประโยคนั้นเคยเขียนว่า *"ติดป้ายได้เมื่อ
> ทั้งสามแพลตฟอร์มตามทันแล้ว"* ตอนนี้เกณฑ์เป็นของ **Windows** และสามแพลตฟอร์มย้ายไปเป็น
> เงื่อนไขของ**การเปิดตัว** ไม่ใช่ของเลขเวอร์ชัน — เหตุผลอยู่ที่ [docs/DECISIONS.md §109](docs/DECISIONS.md)
>
> **เอกสารนี้เป็นเจ้าของแค่สองอย่าง: ลำดับ และเส้นแบ่งว่าอะไรอยู่ในรุ่นไหน**
> รายละเอียดอยู่กับเอกสารที่เป็นเจ้าของเรื่องนั้นเสมอ — สถานะแพลตฟอร์มอยู่ที่
> [PLATFORM-SUPPORT.md](PLATFORM-SUPPORT.md) · เหตุผลของการตัดสินใจอยู่ที่
> [docs/DECISIONS.md](docs/DECISIONS.md) · ช่องว่างของแต่ละประตูอยู่ที่
> [docs/DOOR-CODE.md](docs/DOOR-CODE.md) และ [docs/DOOR-ASSISTANT.md](docs/DOOR-ASSISTANT.md) ·
> สถานะเทสต์อยู่ที่ [TEST-REPORT.md](TEST-REPORT.md)
> ถ้าเอกสารนี้ขัดกับเอกสารเจ้าของเรื่อง **เอกสารเจ้าของเรื่องชนะ** และเอกสารนี้คือฝ่ายที่ต้องแก้

## กติกาข้อเดียวที่คุมทั้งแผน

**เวอร์ชันไม่ใช่กล่องใส่ฟีเจอร์** — ฟีเจอร์เสร็จเมื่อไหร่ปล่อยเมื่อนั้น ไม่ต้องรอให้ครบชุด
ห้ามกลั้นของที่พร้อมแล้วไว้เพื่อให้รุ่นดู "ใหญ่พอ" เพราะของที่กองรอคือของที่ยังไม่มีใครใช้
และรุ่นใหญ่คือรุ่นที่บั๊คหายากที่สุด

ต่อจากเดิม (30 ก.ค.): *"ship then extend"* — **1.0.0 แปลว่า "ผมสัญญาว่าจะไม่ทำของคุณพัง"**
ไม่ได้แปลว่าฟีเจอร์ครบ · ครึ่งประโยคที่เติมเมื่อ 9 ส.ค. — *"และมันทำงานได้ไม่ว่าคุณอยู่บนอะไร"* —
ยังเป็นคำสัญญาอยู่ แต่ **15 ส.ค. ย้ายไปผูกกับการเปิดตัว ไม่ใช่กับเลข 1.0.0** เพราะคำสัญญาว่า
จะไม่ทำของคุณพังนั้น เป็นจริงกับคนที่ใช้อยู่ตอนนี้ได้แล้ววันนี้ และการกลั้นไว้ไม่ได้ทำให้
อีกสองแพลตฟอร์มมาถึงเร็วขึ้นแม้แต่วันเดียว

---

## สาย 0.9.x — บน Windows ปล่อยถี่

### ปิดหนี้ความซื่อสัตย์ (ทำก่อนอย่างอื่นเสมอ)

คำสัญญาแกนของ 1.0.0 คือ**ตัวเลขที่วัดได้เป็นความจริง** ตอนนี้มีที่เพี้ยนอยู่ และทุกอันคือของที่
คนตรวจสอบเรากดดูได้ใน 10 วินาที

- ตัวเลข RAM / เวลาเปิด / จำนวน process วัดครั้งสุดท้าย 27 ก.ค. บน v0.9.2 ยังไม่วัดใหม่
  ([BENCHMARK.md](BENCHMARK.md) มีกติกาการวัด)
- ขนาดคู่แข่งในตาราง [BENCHMARK.md](BENCHMARK.md) §4 เป็นของ 27 ก.ค. ทุกตัวออกเวอร์ชันใหม่ไปแล้ว

ปิดไปแล้ว: ขนาดของ Aetox (วัด 25 ส.ค. บน v1.5.7 = 48.5 MB) · จำนวนเทสต์ใน README และบนเว็บ ·
[TEST-REPORT.md](TEST-REPORT.md) เลิกเขียนจำนวนเทสต์ลงกระดาษตั้งแต่ 7 ส.ค.

### ของที่ดึงมาจาก "หลัง 1.0.0" เพราะช่องว่างเกิดขึ้นแล้ว (ตัดสิน 9 ส.ค.)

**ทุกแถวต้องบอกประตู** — ของชิ้นเดียวกันบนคนละประตูคือคนละงาน และ "ไม่ระบุ" แปลว่า
จะไปเถียงกันตอนลงมือ ([COMPANY.md §2](COMPANY.md))

| ของ | ประตู | ทำไมดึงมา | สถานะฐาน |
|:--|:--|:--|:--|
| **งานเบื้องหลังข้ามเทิร์น** | **ทั้งสอง** — กลไก `task` อยู่หมวด `agent` ซึ่งอยู่บนทั้งโต๊ะผู้ช่วยและโต๊ะโค้ด | *"ฝากไว้แล้วเดินจากไป"* — สั่งงานยาวแล้วคุยเรื่องอื่นต่อได้ ปิดหน้าต่างแล้วงานยังวิ่ง | ตัวรัน + ชิป + การต่อกลับหลังรีเฟรช **มีครบแล้ว** ขาดแค่อายุที่ไม่ผูกกับเทิร์น + ที่เก็บสถานะ |
| **เอเจนชั่วคราว** | **ทั้งสอง — แต่เป็นลูกน้องใต้เพดานโต๊ะที่เรียก ไม่ใช่การจ้างข้ามโต๊ะ** (COMPANY.md §6 ข้อ 4 ยังแข็งเท่าเดิม: โต๊ะโค้ดจ้างใครไม่ได้ ใครจ้างมันไม่ได้) | แต่งลูกน้องขึ้นมาสดสำหรับงานขนาน ไม่ต้องมีไฟล์โปรไฟล์ก่อน · เป็นฐานที่ทีมเอเจนของประตูโค้ดต้องใช้ | `FilterRegistry` + เพดานสิทธิ์ + ตัวรันขนาน มีครบ ขาด "Profile ที่เกิดจากอาร์กิวเมนต์" |
| **ทีมเอเจนของประตูโค้ด** | โค้ด | หน้าโค้ดยังโล่ง และเมนตัวเดียวคุมทุกอย่างคือคอขวด | ดู §ทีมเอเจน ข้างล่าง |
| **ตัวเชื่อมออโตเมชันภายนอก** | ผู้ช่วย — **ห้องทำงานอัตโนมัติคือหน้าที่ใช้เชื่อม** (เจ้าของเคาะ 9 ส.ค.) | ไม่ฝังเครื่องมืออัตโนมัติไว้ในตัว แต่ให้ Aetox เป็น**มือที่ไปใช้ของที่ผู้ใช้มีอยู่แล้ว** · **รองรับหลายเจ้า ไม่ผูกเจ้าเดียว** — n8n หรือ Windmill | ระบบ "การเชื่อมต่อ" (`internal/connect`) รับได้เลย ขาดแค่ช่อง base URL · ต้องออกแบบให้ตัวเลือกเจ้าที่สองไม่ใช่การเขียนใหม่ |
| **พรีเซ็ตผู้ช่วยส่วนตัว 4 ตัว** | ผู้ช่วย | `/สรุปประชุม` `/สลิป` `/จัดไฟล์` `/ตามราคา` — พรีเซ็ตที่มีตอนนี้เป็นของเดฟทั้งหมด ขัดกับคำที่ README เขียนว่า "สำหรับทุกคน" | เครื่องมือที่ต้องใช้มีครบแล้ว เหลือเขียนไฟล์ · **ชุดคำสั่งเป็นของใช้ร่วมทั้งแอป** (COMPANY.md §5) — ต้องตัดสินตอนเขียนว่าโผล่ที่ประตูโค้ดด้วยไหม |

### ทีมเอเจน — ประตูโค้ดเท่านั้น

**ลูป:** สำรวจของเดิม → **ออกแบบสถาปัตยกรรม** → ผู้ใช้อนุมัติแบบ → แตกเป็นงานที่ขนานได้ →
ทีมลงมือ → ประกอบและตรวจ → ทีมสลาย (ทิ้งเอกสารออกแบบไว้) → เมนดูแลต่อตลอดชีวิตที่เหลือ

**สถาปัตยกรรมไม่ใช่ขั้นตอนหนึ่งในลูป — มันคือสิ่งที่ทำให้การขนานเป็นไปได้** เอเจนห้าตัว
ทำงานพร้อมกันได้ก็ต่อเมื่อสัญญาระหว่างชิ้นถูกตัดสินไปแล้ว ถ้าไม่มีใครตัดสิน พวกมันต้อง
คุยกันเองระหว่างทาง และการคุยกันคือการทำงานแบบต่อคิว — ขนานตายทันที **การขนานซื้อมาด้วย
การออกแบบล่วงหน้า ไม่ใช่ด้วยจำนวนเอเจน** และสถาปัตยกรรมคือสิ่งเดียวที่บอกได้ว่าใครถือ
ขอบเขตไหน จึงเป็น**แผนที่ของสิทธิ์การเขียนไฟล์**โดยตัวมันเอง (ดูกฎขอบเขตข้างล่าง)

กฎที่ทำให้มันไม่พัง:

- **ออกแบบเป็นเอกพจน์ ลงมือเป็นพหูพจน์** — สถาปนิกคือ *ตัวเดียว* ไม่ใช่ทีม
  สถาปัตยกรรมที่ออกแบบโดยคณะกรรมการคือสถาปัตยกรรมที่ไม่มีความคิดเดียว ส่วนการลงมือ
  ต่างหากที่ขนานได้ · วินัย "สำรวจก่อนออกแบบ ทำแผนที่ก่อนตัดสิน" มีอยู่แล้วใน
  [senior-architect-agent](https://github.com/aetox-skills/senior-architect-agent)
- **ผู้ใช้อนุมัติ "แบบ" ไม่ใช่ "ตารางงาน"** — คนอนุมัติสนใจว่ากำลังจะสร้างอะไรและมันจะ
  ต่อกันยังไง ไม่ได้สนใจว่าเอเจนตัวไหนถือไฟล์ไหน · การแตกเป็นงานขนานเกิด*หลัง*อนุมัติ
  และอนุมานมาจากแบบ
- **เอกสารออกแบบคือมรดกที่ทีมทิ้งไว้** — ทีมสลายแล้วไม่มีใครให้ถาม แต่แบบยังอยู่
  นี่คือคำตอบของ "เมนรับมรดกโค้ดที่ไม่ได้เขียนเอง": มันไม่ได้รับแค่โค้ด มันรับเหตุผลด้วย

- **เรียกได้ครั้งเดียวต่อหนึ่งแผนที่ผู้ใช้อนุมัติ** — ไม่ใช่ครั้งเดียวต่อโปรเจกต์ (ระบบย่อยใหม่
  ในโปรเจกต์เดิมก็เป็นงานรูปเดียวกัน) จบแผน ทีมสลาย อยากได้อีกต้องมีแผนใหม่ที่อนุมัติใหม่
  **ตั้งใจไม่มีปุ่มเรียกทีมกลับ**
- **เกณฑ์จบคือ "ประกอบแล้วใช้งานได้" ไม่ใช่ "ต่างคนต่างเขียนไฟล์เสร็จ"** — ถ้าประกอบไม่ติด
  ทีมซ่อมของตัวเองในรอบเดียวกันก่อนสลาย ไม่งั้นเมนรับมรดกกองโค้ดที่ไม่มีใครเหลือให้ถาม
- **ลองใหม่ระหว่างรอบ ≠ เรียกทีมกลับ** — ลูกน้องที่ตายกลางทางต้องมีตัวแทนในรอบเดิม
  ไม่งั้นแผนเสร็จไม่ครบแบบเงียบ ๆ
- **ขอบเขตการเขียนไฟล์บังคับด้วยโครงสร้าง ไม่ใช่ด้วยข้อความในพรอมป์** — แผนบอกว่าใคร
  ถือไฟล์ไหน แล้วสร้าง `safety.PermissionRule` ต่อลูกน้องจากแผนนั้น
  (`internal/subagent/task.go` ประกอบกฎต่อลูกน้องอยู่แล้ว — เป็นการเติมข้อมูล ไม่ใช่สร้างกลไกใหม่)
  ปัจจุบันตัวกันเดียวคือพรอมป์ ซึ่ง `internal/subagent/runner.go` เตือนตัวเองไว้แล้วว่า
  *"มันนับลูกน้อง ไม่ได้นับคนเขียน"*
- **git worktree ยังไม่ทำ** — มันชนะเฉพาะตอนลูกน้องต้อง*รัน*ของขนานกัน (สอง `npm install`
  ในโฟลเดอร์เดียว, พอร์ตชนกัน) หรือตอนต้องทิ้งงานของตัวหนึ่งยกก้อน · ขอบเขตไฟล์ได้ผล ~90%
  ด้วยแรงหนึ่งในสิบ และใช้ได้กับโปรเจกต์ที่ไม่ใช่ repo ด้วย

**เหตุผลของรูปนี้:** การขนานได้กำไรเฉพาะงานที่เป็นอิสระต่อกัน และ*เสียหาย*กับงานที่ต้องทำ
ต่อกันเป็นทอด ๆ — งานตอนเริ่มสร้างคือจุดที่ขนานได้ดีที่สุด ส่วนแก้บั๊คคือจุดที่ขนานได้แย่ที่สุด
และทีมเอเจน**แพงขึ้น ไม่ใช่ถูกลง** (สามตัวขนานกิน token ราว 7 เท่า) การคุมค่าใช้จ่ายจึงคือ
ทำให้ทีม*เกิดขึ้นนาน ๆ ครั้ง* ไม่ใช่ทำให้ทีมถูก

ผลพลอยได้ที่ไม่ต้องออกแบบเพิ่ม: **เมนกลายเป็นผู้ตรวจที่ไม่ใช่ผู้เขียนโดยอัตโนมัติ** —
มันอ่านโค้ดนั้นด้วยสายตาสดตั้งแต่วันแรก ซึ่งเป็นกลไกคุมคุณภาพที่ได้ผลจริง

> **แผนที่ผู้ใช้อนุมัติ = plan mode** ที่ [DECISIONS.md §44](docs/DECISIONS.md) เคยตัดสินใจ
> "เก็บไว้เป็นอนาคต ตั้งใจไม่สร้าง" พร้อมระบุทางกลับมา (เป็น *mode* ไม่ใช่เอเจนให้เลือก
> ใช้ `Profile.DenyRules` + `FilterRegistry` เดิม) และโค้ดที่ลบไปอยู่ใน commit `6f7ad46`
> — **plan mode กับทีมเอเจนคือฟีเจอร์เดียวกัน ไม่ใช่สองอัน**

### ประตูผู้ช่วย — ช่องว่างที่ยังไม่มีใครรับ

[DOOR-ASSISTANT.md §6](docs/DOOR-ASSISTANT.md) ลิสต์ไว้ตั้งแต่ 5 ส.ค. แต่ไม่เคยขึ้นแมพ —
ขณะที่ช่องว่างฝั่งประตูโค้ดขึ้นครบทุกอัน **แมพที่เอียงไปข้างเดียวคือแมพที่พาไปผิดที่**

- **ตัวช่วยใช้งานครั้งแรก** — ประตูนี้วัดความสำเร็จที่ *"คนที่ไม่รู้ว่าโฟลเดอร์คืออะไร
  ก็ใช้ได้และได้งาน"* ([DOOR-ASSISTANT.md §0](docs/DOOR-ASSISTANT.md)) แล้วเส้นทางไป 1.0
  ไม่มีมันอยู่เลย · คู่กับข้อความขออนุมัติภาษาคนข้างล่าง — สองอันนี้คืออันเดียวกันในสายตา
  ผู้ใช้กลุ่มนั้น ให้การติดตั้งสดตัดสินว่าต้องมาก่อน 1.0 ไหม
- **หน้าจัดการ/แก้ไขเอเจนในแอป** — วันนี้แก้ที่ไฟล์ `AGENT.md` เอง ซึ่งขัดกับ
  *"จ้างเอเจนใหม่ = วางไฟล์เพิ่มหนึ่งไฟล์"* ที่ตั้งใจให้เป็นเรื่องง่าย ([COMPANY.md §4](COMPANY.md))
- **ทางดูว่าเอเจนตัวหนึ่งเรียนรู้อะไรไปแล้ว** — `agents/<ชื่อ>/MEMORY.md` มีของอยู่จริง
  แต่ไม่มีหน้าจอให้เปิด (ซึ่งทำให้ลูปการเรียนรู้ขาดคนตรวจ)

### หนี้ที่ควรปิดในสายนี้

- `internal/mcp/client.go` — MCP server ที่ fork ลูก (`npx→node`, `uvx→python`) อาจทิ้ง
  โปรเซสหลานค้างบนเครื่องผู้ใช้ **นี่คือ ponytail ตัวเดียวที่รั่วของจริงบนเครื่องคนอื่น**
  ทางแก้เขียนไว้ในโค้ดแล้ว (process group + kill group)
- `internal/orchestrator` — ไม่มีใครเรียกใช้เลยสักที่ ([DECISIONS.md §10](docs/DECISIONS.md)
  ระบุว่า "เสนอไว้ ไม่ได้สร้าง") **ตัดสินใจก่อนติดป้าย 1.0: ต่อสาย หรือลบทิ้ง**
- ~~ปุ่ม `kind: 'soon'` ควรซ่อนก่อน 1.0~~ **ถอนแล้ว (เจ้าของเคาะ 9 ส.ค.)** — ห้องที่ยังว่าง
  วาดไว้ตาม [COMPANY.md §2](COMPANY.md): *"ปุ่มที่โผล่มาทีหลังอ่านเหมือนฟีเจอร์ใหม่
  ปุ่มที่อยู่ตั้งแต่แรกอ่านเหมือนแผนที่ทำเสร็จ"* · `kind: 'soon'` เป็นชนิดห้องที่ถูกต้อง
  ไม่ใช่หนี้ที่ต้องปิด
- `shell.assistantBlurb` / `shell.codeBlurb` ยังเป็นข้อความ placeholder —
  [HANDOFF.md](HANDOFF.md) ระบุว่าเจ้าของจะเขียนเอง **งานนี้มีแต่เจ้าของทำได้**
- ข้อความขออนุมัติยังเป็นคำสั่งดิบ — [DOOR-ASSISTANT.md](docs/DOOR-ASSISTANT.md) เขียนเองว่า
  *"สำคัญที่สุดในรายการนี้"* สำหรับกลุ่มผู้ใช้ที่ประกาศไว้ ให้การทดสอบติดตั้งสดเป็นคนตัดสินว่า
  เจ็บพอที่จะทำก่อน 1.0 ไหม

### ทางแยกถัดไป — ยังไม่เคาะ

**คำถามเดียว: ประตูไหนได้เวลาของเจ้าของก่อน** งานพอร์ตแพลตฟอร์มเดินคู่ไปอยู่แล้วและไม่เกี่ยวกับข้อนี้

| | **ประตูผู้ช่วย** | **ประตูโค้ด** |
|---|---|---|
| ของที่จะทำ | ข้อความอนุมัติภาษาคน · ตัวช่วยใช้งานครั้งแรก · พรีเซ็ต 4 ตัว | ทีมเอเจน (ลูปสถาปนิก) · ประตูส่งไม้ |
| ระยะห่างจากเป้าของประตูตัวเอง | ~40% — *"คนที่ไม่รู้ว่าโฟลเดอร์คืออะไรก็ใช้ได้"* ยังไม่จริง | ~80% — นักพัฒนานั่งทำงานได้วันนี้ |
| ราคา | ถูก — ROADMAP เขียนเองว่าเครื่องมือมีครบแล้ว เหลือเขียนไฟล์ | แพงที่สุดบนแมพ — **กิน token ~7 เท่า** |
| ปลดล็อกอะไร | คลิปเดโม 30 วิ (เงื่อนไข "ต้องมีก่อนโพสต์") → **สายไทยยิงได้ทันที** ไม่ต้องรอสามแพลตฟอร์ม | เจ้าของสร้าง Aetox ได้เร็วขึ้น |
| ข้อควรระวัง | — | คอขวดย้ายจาก "เขียนไม่ทัน" เป็น **"ตรวจไม่ทัน"** ซึ่งแย่กว่า · และได้กำไรเฉพาะตอนเริ่มสร้างของใหม่ |

**ข้อเท็จจริงหนึ่งข้อที่เพิ่งเปลี่ยนสมการ (§92):** ผู้ช่วยทำงานซอฟต์แวร์ได้แล้วและไม่ปฏิเสธ
แปลว่ามันจะรัน shell / ติดตั้งของ / แตะไฟล์บ่อยขึ้น — และทุกครั้งคือข้อความอนุมัติที่โชว์คำสั่งดิบ
ให้กลุ่มเป้าหมายที่อ่านไม่ออกอ่าน **การตัดสินใจเมื่อวานสร้างหนี้ให้ประตูผู้ช่วยโดยตรง**
เหตุผลตรงข้ามที่หนักที่สุดคือทีมเอเจนทำให้เจ้าของสร้างของได้เร็วขึ้น ซึ่งจริง แต่เป็นการลับมีด
ที่จ่าย 7 เท่าเพื่อผู้ใช้ที่ยังไม่มี

---

## การพุชแพลตฟอร์ม — งานตามให้ทัน

**กติกา: งานพอร์ตไม่ถ่วงสายฟีเจอร์** ฟีเจอร์ลง Windows ก่อนเสมอ แล้วแพลตฟอร์มอื่นตามมา
ไม่ใช่รอให้ครบสามแล้วค่อยปล่อย · Windows คือแพลตฟอร์มอ้างอิง — เมื่อพฤติกรรมสองแพลตฟอร์ม
ต่างกันโดยไม่มีเหตุผลของ OS รองรับ **ฝั่ง Windows คือคำตอบที่ถูก**

รายละเอียดและตารางเฟสทั้งหมดอยู่ที่ [PLATFORM-SUPPORT.md](PLATFORM-SUPPORT.md) —
ที่นี่บอกแค่**ลำดับ**:

1. **Linux ก่อน** — เฟส 3a (`browser_linux.go`, WebKitGTK) ง่ายกว่าเพราะ Wails ใช้ WebKitGTK
   อยู่แล้ว, CI ฟรี, ไม่ต้อง notarize · engine/CLI/desktop ผ่านเทสต์บนเคอร์เนล Linux จริงแล้ว
2. **macOS ตามมา** — เริ่มที่**เทสต์แดงสองตัวที่ค้างมาตั้งแต่ ~4 ส.ค.** ซึ่ง PLATFORM-SUPPORT.md
   ระบุว่าเป็น "รายการแรกของงานพอร์ต macOS ไม่ใช่ 3b" · ต้องมีเครื่อง Mac จริง (รันใน VM
   บนเครื่องที่ไม่ใช่ Apple ผิดสัญญาอนุญาต) · แล้วค่อยเฟส 3b
3. **เฟส 4 packaging** — `.deb`/`.rpm`/tar.gz + `.app`/`.dmg` · macOS ต้อง notarize
   (= บัญชี Apple Developer) · `release.yml` ตอนนี้เป็น `runs-on: windows-latest` อย่างเดียว
4. **`internal/atrest`** — DPAPI มีแต่ฝั่ง Windows แปลว่า**รหัสผ่านเก็บเป็นข้อความเปล่าบนอีกสอง
   แพลตฟอร์ม** ต้องมี Keychain (macOS) และ libsecret/เทียบเท่า (Linux) **นี่คือตัวขวางเชิง
   ความปลอดภัย ปล่อยไม่ได้** และแพ็กเกจนี้ยังไม่มีเทสต์เลยสักตัว

## 1.0.0 — เกณฑ์ ไม่ใช่วันที่

**แก้ 15 ส.ค. 2026:** เกณฑ์ชุดนี้เคยขึ้นต้นทุกข้อด้วยคำว่า "ทั้งสามแพลตฟอร์ม" ตอนนี้เป็นเกณฑ์
ของ **Windows** ข้อที่พูดถึงอีกสองแพลตฟอร์มไม่ได้ถูกลบ มันย้ายลงไปอยู่ในรุ่นถัดไปทั้งข้อ
([docs/DECISIONS.md §109](docs/DECISIONS.md))

ติดป้าย 1.0.0 ได้เมื่อทุกข้อเป็นจริง:

- [ ] Windows ติดตั้งได้ เปิดได้ และแท็บ browser ใช้งานได้จริง
- [ ] ความลับถูกเข้ารหัสที่พักบน Windows (DPAPI)
- [ ] ตัวเลขที่เผยแพร่ทุกตัวตรงกับของจริง (ขนาด, เทสต์, RAM, เวลาเปิด)
- [ ] ไม่มีข้อความ placeholder เหลือบนหน้าจอ · ห้อง `kind: 'soon'` **ไม่นับเป็น placeholder**
      — มันคือรูปร่างของผลิตภัณฑ์ที่ตั้งใจวาด (COMPANY.md §2)
- [ ] ทุกอย่างที่ 1.0 สัญญาว่าทำได้ ผ่านการติดตั้งสดแบบคนแปลกหน้าอย่างน้อยหนึ่งรอบ

**ช่องติ๊กเป็นของเจ้าของ** เอกสารนี้ไม่ติ๊กแทน — ข้อ "ตัวเลขที่เผยแพร่ทุกตัวตรงกับของจริง"
ยังค้างที่ RAM กับเวลาเปิด ซึ่ง [BENCHMARK.md](BENCHMARK.md) วัดครั้งสุดท้าย 27 ก.ค. บน v0.9.2
ส่วนขนาดวัดใหม่แล้วเมื่อ 25 ส.ค. บน v1.5.7

### รุ่นถัดไป — สามแพลตฟอร์ม

- [ ] Linux กับ macOS ติดตั้งได้ เปิดได้ และแท็บ browser ใช้งานได้จริง
- [ ] ความลับถูกเข้ารหัสที่พักบน Linux (libsecret) และ macOS (Keychain) — **ตัวขวางเชิงความปลอดภัย**
- [ ] ผ่านการติดตั้งสดแบบคนแปลกหน้าอย่างน้อยหนึ่งรอบต่อ OS

---

## ตั้งใจไว้ทีหลัง — และเหตุผล

| ของ | ทำไมยังไม่ทำ |
|:--|:--|
| **Workflow เป็นสคริปต์** | เจ้าของยังไม่เห็นภาพ · ตัวสร้าง workflow แบบภาพเหมาะกับกลุ่มผู้ใช้มากกว่า |
| **ตัวตั้งเวลา / cron ในตัว** | **ไม่สร้าง — เจ้าของเคาะ 9 ส.ค.** ห้องทำงานอัตโนมัติทำงานได้ก็ต่อเมื่อเชื่อมเครื่องมือภายนอก (n8n / Windmill) เจ้าพวกนั้นมี cron/webhook/trigger ครบแล้ว **ปล่อยให้มันเป็นนาฬิกา Aetox เป็นมือ** · ยังไม่เชื่อม = ห้องยังกดไม่ได้ ซึ่งเป็นคำตอบเดียวกับที่ปุ่มบอกอยู่แล้ว |
| **ประตูส่งไม้ (ผู้ช่วย → โค้ด)** | ออกแบบไว้แล้วที่ [COMPANY.md §3](COMPANY.md) · เลื่อนเป็นชุดหลัง |
| **รีวิว PR** | [DOOR-CODE.md §5](docs/DOOR-CODE.md) เขียนเองว่า *"อันเดียวที่ขาดจริง"* — ต้องมี Decision ของตัวเองก่อน |
| **โต๊ะ / ห้องใหม่** | [COMPANY.md §7](COMPANY.md): *"อย่าจับปลาหลายมือ เดะจะตายก่อน"* |

## เรื่องเปิดตัว

**รอเปิดตัวใหญ่จนกว่าจะครบสามแพลตฟอร์ม — ไม่ใช่จนกว่าจะถึงเลขเวอร์ชันไหน** (แก้ 15 ส.ค.
2026: ประโยคนี้เคยผูกการเปิดตัวไว้กับ "1.0.0 สามแพลตฟอร์ม" ซึ่งตอนนี้เป็นคนละเรื่องกันแล้ว —
1.0.0 ออกบน Windows ได้ โดยไม่ใช่สัญญาณให้ไปโพสต์ที่ไหน) ไม่ใช่เพราะกลัว แต่เพราะกลุ่มที่จะพาเราไปไกล
ที่สุด (r/LocalLLaMA, Hacker News) ใช้ Mac และ Linux เป็นหลัก ยิงตอน Windows-only คือ
การเสียคนครึ่งหนึ่งไปในคอมเมนต์แรก · ระหว่างนี้สายไทยเดินได้เลย เพราะ Aetox พูดไทยและ
คนสร้างเป็นคนไทย — สนามที่เราไม่มีคู่แข่ง

**ต้องมีก่อนโพสต์ที่ไหนก็ตาม:** คลิปเดโม 30 วินาที (สั่งเป็นภาษาคน → เห็นมันลงมือ → ได้ไฟล์จริง)
และการเซ็นโค้ด (Azure Trusted Signing) เพราะจอแดง SmartScreen อ่านว่า "ไวรัส" สำหรับกลุ่ม
ผู้ใช้ที่เราตั้งใจจะได้มากที่สุด
