# 🚀 Aetox v1.2.0 — โปรเจกต์นี้ตกลงกันว่าอะไร และผู้ใช้ชี้ได้แล้ว

รุ่นนี้เพิ่มความจำอีกชั้นที่ **ไม่** ข้ามโปรเจกต์ เอาการตัดสินใจเรื่องความจำมาไว้ใต้คำตอบที่เสนอมัน
สอนหน้าจอให้วาดคณิตศาสตร์ ให้ผู้ใช้ชี้อิลิเมนต์บนหน้าเว็บส่งให้เอเจน และให้เอเจนประกาศได้ว่างาน
ที่รับไปมีกี่เฟส ก่อนที่เฟสแรกจะเริ่ม พร้อมกับปิดรูความปลอดภัยหนึ่งรู และซ่อมสองอย่างที่ทำให้
เอเจนโทษตัวเองในสิ่งที่ไม่ใช่ความผิดมัน

ครึ่งหลังของรุ่นเป็นเรื่องน้ำหนักกับความจริง: บล็อกเครื่องมือเบาลง **23%** โดยที่โมเดลไม่ได้ถูก
บอกน้อยลง การมอบงานกลายเป็นสวิตช์ที่ **ส่งมาในสถานะปิด** ภาษาที่สามเขียนได้แล้วและจีนตัวย่อ
เป็นตัวพิสูจน์ หน้าติดตั้งถูกรื้อทั้งชุดเพราะไม่มีใครที่ทำแอปนี้เคยเห็นมัน และมิเตอร์คอนเท็กซ์
เลิกวาดตัวส่วนที่ไม่มีใครเคยส่งมาให้

## 1. 🧠 การเรียนรู้ข้ามโปรเจกต์ การตัดสินใจไม่ข้าม

"เครื่องนี้ไม่มี Excel" เป็นจริงไม่ว่าคุณจะนั่งอยู่โฟลเดอร์ไหน — ความจำแบบนั้นข้ามโปรเจกต์มาตั้งแต่
§82 และยังข้ามเหมือนเดิม สิ่งที่ไม่เคยมีบ้านคือของอีกชนิด: **"ที่นี่เราตกลงกันว่า X และเพราะอะไร"**

โต๊ะรับไม่ได้ เพราะโต๊ะโค้ดคือโต๊ะเดียวกันในทุกรีโป สิ่งที่เก็บไว้ที่นั่นจะเดินไปโผล่เป็นคำแนะนำใน
โปรเจกต์ถัดไป และ `AETOX.md` ก็ไม่ใช่ เพราะนั่นคือสิ่งที่ **คุณ** บอกเอเจน ส่วนนี่คือสิ่งที่ **งาน**
สรุปได้แล้วคุณอนุมัติ — คนละคนเขียน คนละคำถาม

ตอนนี้ `memory` มีปลายทางที่สาม เอเจนเลือกได้ว่าจะจำลงส่วนกลาง ลงโต๊ะ หรือลงโปรเจกต์ที่กำลัง
โฟกัสอยู่ ไฟล์อยู่ใต้ data root ของ Aetox ไม่ใช่ในรีโปของคุณ (ไม่มี git noise ที่ไม่มีใครขอ และ
ทำงานกับโฟลเดอร์ที่ไม่ใช่รีโปด้วย) คีย์คือชื่อฐานที่อ่านออกบวกแฮชของพาธ — คีย์เดียวกับที่ประวัติ
แชทของโปรเจกต์นั้นใช้อยู่แล้ว ประวัติกับความจำจึงไม่มีทางไม่ตรงกัน

ลำดับในพรอมป์ตคือลำดับความสำคัญ: ความจำของโต๊ะ → สิ่งที่โปรเจกต์นี้ตกลงกัน → `AETOX.md`
ของทีม และ **เซสชันที่ไม่โฟกัสอ่านไม่เห็นชั้นนี้เลย** เพราะมันหยั่งรากที่ทั้งเครื่อง ความจำที่ผูกกับ
โฟลเดอร์นั้นคือลิ้นชักรวมที่ทุกเซสชันเร่ร่อนใช้ร่วมกัน

หน้า Settings แยกรายการให้ตามเจ้าของไฟล์ คุณจึงเห็นว่าบรรทัดไหนเป็นของใคร

## 2. ✅ สิ่งที่เอเจนขอจำ ถูกตัดสินตรงที่มันขอ

`memory` เป็นเครื่องมือเดียวที่งานของมัน **ยังไม่เกิด** ตอนมันรันเสร็จ — มันจ่อแถวไว้แล้วรอคน
แต่ไทม์ไลน์ปฏิบัติกับมันเหมือนเครื่องมืออื่นทุกตัว คุณจึงเห็นแค่ว่า "มีอะไรชื่อ memory รันไป" ส่วน
สิ่งที่มันอยากจำ กับคำตอบรับ/ไม่รับที่มันรออยู่ ไปอยู่อีกสองหน้าถัดไป **ประตูอนุมัติที่ไม่มีใครยืน
อยู่หน้ามัน ไม่ได้ถือสัญญาอะไรเลย มันแค่ไม่เคยเปิด**

ตอนนี้มีการ์ดอยู่ใต้คำตอบที่เสนอมัน อ่านข้อความ กดรับหรือไม่รับได้ที่นั่น การ์ดถือ **id** ไม่ใช่
สำเนาข้อความ เพราะบทสนทนาถูกแช่แข็งแต่คิวยังเดินต่อ — ข้อเสนอที่คุณอนุมัติจากหน้า Settings
เมื่อสัปดาห์ก่อน จะไม่กลับมายื่นปุ่ม "อนุมัติ" ให้กดอีกในเซสชันที่เปิดวันนี้

และการ์ด **ขอ** ไม่ใช่ **ประกาศ** เครื่องมืออื่นเขียนว่า "Saved a memory" ซึ่งจริงในที่ของมัน
แต่ที่นี่จะเป็นคำโกหก: ณ วินาทีนั้นยังไม่มีอะไรถูกบันทึก และการจะมีหรือไม่เป็นสิทธิ์ของคุณ

## 3. 💬 คำตอบที่คุณพิมพ์ทับ ก็ยังเป็นคำตอบ

พิมพ์แทรกตอนเทิร์นกำลังเดิน คำแทรกจะวิ่งเข้าเทิร์นนั้นเลย และถ้าโมเดลเขียนคำตอบเสร็จแล้ว
เทิร์นจะเดินต่อแทนที่จะให้คุณรอคำตอบที่ตัวเองเลยผ่านไปแล้ว ปัญหาคือถัดไปหนึ่งขั้น มันแยกจาก
คำบรรยายไม่ออก — คำตอบที่เขียนจบแล้วจึง **เสียมาร์กดาวน์ เสียขนาด เสียสี และเสียที่ทาง**
ในขั้นตอนเดียว ตรงวินาทีที่คุณกำลังอ่านมันอยู่ (`##`, `**`, `>` โผล่เป็นซอร์สในตัวอักษรจาง 11px)

ข้อบกพร่องสามข้อคือข้อเดียว: ข้อเท็จจริงที่แยกสองสิ่งนี้ออกจากกันถูกรู้อยู่ที่เดียวและไม่เคยเดินทาง
ไปไหน ตอนนี้มันเดินทางไปกับรอบนั้น และติดไปกับสิ่งที่ถูกบันทึกด้วย เซสชันที่เปิดพรุ่งนี้จึงยังแยก
คำตอบออกจากคำบรรยายได้

## 4. ➗ คณิตศาสตร์ถูกวาด เพราะมันไม่เคยเป็นร้อยแก้ว

โมเดลที่ถูกถามเรื่องอินทิกรัลเขียน LaTeX เพราะคณิตศาสตร์เขียนด้วยสิ่งนั้น หน้าจอนี้ไม่เคยรู้
มาร์กดาวน์จึงอ่านสมการเป็นร้อยแก้วแล้วพิมพ์เศษที่เหลือออกมา

ตอนนี้สมการถูกแยกออกมา **ก่อน** มาร์กดาวน์อ่านอะไรทั้งนั้น แล้ว KaTeX วาด รับสี่ตัวคั่น —
`\[..\]`, `$$..$$`, `\(..\)`, `$..$` — เพราะโมเดลเขียนทั้งสี่แบบ และปลายทางไม่ได้เป็นคนเลือก
ตัว `$` เดี่ยวมีเงื่อนไขกันไว้ เพราะ `$` คือเงินด้วย: *"ราคา $5 และ $10"* คือประโยค ไม่ใช่สมการ

สิ่งที่ KaTeX อ่านไม่ออกจะถูกแสดงเป็นซอร์ส **ไม่ใช่กรอบแดง** — สมการครึ่งเดียวคือ syntax error
อยู่ไม่กี่เฟรมระหว่างสตรีม และไม่ควรมีอะไรแดงวาบระหว่างทางไปสู่ความถูกต้อง ไทยใน `\text{}`
ใช้ได้ (ฟอนต์ละตินเป็นของ KaTeX ไทยตกไป Noto Sans Thai) และปุ่มคัดลอกคืนซอร์ส LaTeX เดิม
ไม่ใช่สิ่งที่ตาเห็น

## 5. ▶️ ปุ่มรันมีเพราะเครื่องนี้รันได้ และรันซอร์สโค้ดได้แล้ว

เดิมคำตอบว่า "บล็อกไหนรันได้" อยู่สองที่ที่ตรงกันเพราะมีคนจำได้ว่าต้องแก้ทั้งคู่ และไม่มีฝั่งไหน
ถามเครื่องเลย ปุ่มจึงโผล่บนภาษาที่ไม่ได้ติดตั้งล่าม แล้วกดกลับมาเป็น **โฆษณา Microsoft Store**

ตอนนี้แอปถามครั้งเดียวตอนเปิดว่าเครื่องนี้รันอะไรได้ ปุ่มที่กดแล้วไม่เกิดอะไรจึงไม่ถูกวาดเลย และ
บล็อก Python/Node/Go ก็มีปุ่มรันแล้ว — ไม่ใช่การกระทำที่สอง เพราะไฟล์ถูกเขียนลงแซนด์บ็อกซ์
แล้วส่ง `python <ไฟล์>` เข้าประตูเดียวกับที่โมเดลใช้ ได้ audit log, timeout และสิทธิ์ชุดเดียวกัน

ผลลัพธ์เป็นแผงของตัวเอง: พับเองเมื่อเกินสิบบรรทัด บอกจำนวนบรรทัดกับเวลาที่ใช้ **บอกเมื่อถูกตัด**
(แผงที่เลื่อนไปสุดที่ไม่ใช่สุดคือทางเดียวที่ผลลัพธ์โกหกได้) และถ้าล้มเหลวมีปุ่มขอให้ช่วยแก้ ซึ่งส่ง
ทั้งโค้ดและ error ไป เพราะ error อย่างเดียวไม่ได้บอกว่ารันอะไร

## 6. 👆 ชี้ให้เอเจนดู

เอเจนอ่านหน้าเว็บได้และกดได้อยู่แล้ว สิ่งที่มันทำไม่ได้คือได้ยิน *"ปุ่มสีฟ้ามุมขวาบนอะ"* แล้วรู้ว่า
นั่นคือโหนดไหน ตอนนี้คุณชี้ได้ และสิ่งที่ลงไปในช่องพิมพ์คือ **ตัวโหนด** — selector, กล่องของมัน,
สีที่มันเรนเดอร์ออกมาจริง และมาร์กอัปรอบตัว ลากเพื่อวงพื้นที่ก็ได้ เพราะ *"ตรงบริเวณนี้"* ไม่ใช่
selector — มันคือที่บนภาพที่ถูกเรนเดอร์ ซึ่งวิธีเดียวที่ซื่อสัตย์คือส่งภาพนั้นไป

ภาพถูกเรนเดอร์โดยเอนจิน ไม่ใช่แคปหน้าจอ เพราะการแคปหน้าจอจะได้อะไรก็ตามที่ลอยทับอยู่ติดมา
ด้วย และนั่นคือคำโกหกเกี่ยวกับหน้าเว็บทันทีที่มีอะไรมาทับ

## 7. 🔁 เอเจนประกาศได้แล้วว่างานนี้มีกี่เฟส

การมอบหมายงานแตกขนานอยู่แล้ว สิ่งที่เพิ่มคือเฟสที่ **ยังไม่เกิดขึ้น** โมเดลที่หาเจอแปดอย่างแล้ว
ตัดสินใจเองว่าจะให้ไปตรวจ คือโมเดลที่กำลังทำบุญให้ — รอบตรวจสอบคือสิ่งที่ถูกข้ามพอดีตอนที่
คำตอบดูเสร็จแล้ว และไม่มีอะไรบนจอบอกว่ามันถูกข้าม เพราะไม่เคยมีอะไรบอกว่ามันกำลังจะมี

ตอนนี้เฟสที่ประกาศไว้นั่งอยู่ที่ **0/8** ต่อหน้าคุณจนกว่าจะมีคนทำจริง การ์ดวาดเฟสตามลำดับที่
ประกาศ ไม่ใช่ลำดับที่งานมาถึง หลักประกันคือ**การมองเห็น ไม่ใช่การบังคับ** — Aetox บังคับให้โมเดล
เดินรอบสองไม่ได้ แต่ทำให้การไม่เดินมองเห็นได้ และการ์ดบอกด้วยว่าผู้ช่วยแต่ละตัวรันบนโมเดลอะไร
และใช้โทเคนไปเท่าไร (ยอดรวมเคยรู้ว่าเท่าไร แต่ไม่มีใครรู้ว่าของใคร)

## 8. 🪟 หน้าต่างเปิดในจอที่มันได้รับ ไม่ใช่จอที่มันสมมติ

โน้ตบุ๊ก 1920x1080 ที่สเกล 150% ตามค่าเริ่มต้นของวินโดวส์ คือ 1280x720 ในหน่วยที่หน้าต่างใช้
Aetox จึงถูกสร้างสูงเกินจอ 180px แล้วจัดกึ่งกลางบนมัน — ขอบบนไปอยู่ที่ y = -114 **พร้อมกับ
ปุ่มย่อ/ขยาย/ปิด** ซึ่งเป็นของวินโดวส์ ไม่ใช่ของแอป

ตอนนี้แอปวัดก่อนเปิด รวมทาสก์บาร์ในการวัด (การจัดกึ่งกลางเทียบกับพื้นที่ทำงาน หน้าต่างที่สูงเท่า
*จอ* จึงยังถูกดันพ้นขอบไปครึ่งทาสก์บาร์) และมันมีแต่ **หด** ไม่เคยขยาย จอที่มีที่พอเปิดเท่าเดิม

## 9. 🔒 ความปลอดภัย: เครื่องมือที่เดินไล่ไฟล์ผ่านทะลุรายการที่ถูกปฏิเสธ

`grep`, `glob` และ `fs find` ตรวจสิทธิ์ที่ **พาธฐาน** ครั้งเดียว แล้วส่งทุกไฟล์ใต้มันเข้าไปอ่าน
ไม่มีใครถามซ้ำรายไฟล์ การเดินที่เริ่มจากโฟลเดอร์ที่เซสชันถือได้โดยชอบ จึงอ่านทะลุสิ่งที่ถูกปฏิเสธ
ไว้ข้างใต้ — และในโหมดไม่โฟกัสที่เวิร์กสเปซคือทั้งเครื่อง `credentials.json` กับโฟลเดอร์สกิล
ส่วนตัวของทุกคนงาน อยู่ห่างแค่ `grep` เดียวจากเซสชันที่ถูกปฏิเสธทั้งสองอย่างโดยชื่อ

ปิดแล้ว รายการเดิม คำตัดสินเดิม สำเนาเดียว และไม่มี syscall เพิ่มต่อไฟล์ (แปลง symlink ครั้งเดียว
ที่ฐาน ที่เหลือเป็นเลขคณิตของสตริง — เหตุผลที่การตรวจนี้ไม่ได้อยู่ในตัวเดินไล่ไฟล์ตั้งแต่แรกคือราคา
ของมัน) รายการที่ถูกปฏิเสธถูกข้ามเงียบ ๆ เพราะการประกาศทุก credential store ที่ไม่ยอมอ่าน คือ
การเผยแพร่ตำแหน่งของมันลงบันทึกโดยไม่ได้อะไรเลย และ `glob` กรองด้วย เพราะชื่อไฟล์คือการรั่ว
ที่เล็กกว่าเนื้อไฟล์ แต่ยังเป็นการรั่ว

## 10. 🧯 สองอย่างที่ทำให้เอเจนโทษตัวเองในสิ่งที่ไม่ใช่ความผิดมัน

**Python บนวินโดวส์เขียนด้วยโค้ดเพจของเครื่อง** สคริปต์ที่พิมพ์ `∞` ตายด้วย `UnicodeEncodeError`
แล้วไปถึงเอเจนเป็น `exit status 1` เปล่า ๆ มันอ่านว่า "เลขผิด" แล้วรื้อคำตอบที่ถูกอยู่แล้วทิ้ง (เกิดจริง
สามครั้งใน `shell-audit.log` วันที่ 16 ส.ค.) และอีกแบบเงียบกว่า: cp874 เข้ารหัสไทยได้ สคริปต์ที่
พิมพ์ไทยจึง **exit 0 พร้อมข้อความเพี้ยน** ซึ่งเป็นสิ่งที่เอเจนจับไม่ได้เลย ตอนนี้ทุกคำสั่งได้
`PYTHONUTF8=1` (ข้ามทั้งก้อนถ้าคุณตั้งค่านี้ไว้เองแล้ว)

**WSL อ่านบรรทัดคำสั่งสองรอบ** `--` ยกส่วนที่เหลือให้เชลล์เริ่มต้นของดิสโทรอ่านเป็นบรรทัดคำสั่ง
ของตัวเองก่อน มันจึงขยาย `$…` ทุกตัวไปก่อนที่ bash ของเราจะเห็น ผลที่วัดได้เมื่อ 17 ส.ค.:
`awk '{print $1}'` กลายเป็น `awk '{print }'` (พิมพ์ทั้งบรรทัด ไม่มี error ไม่มีเบาะแส),
`{sum+=$2}` กลายเป็น syntax error ที่ถูกโทษว่าเป็นความผิดโมเดล และ `false; echo rc=$?`
รายงาน `rc=0` — **ความล้มเหลวที่ถูกอ่านเป็นสำเร็จ คือทิศเดียวที่ตัวห่อหุ้มผิดไม่ได้** เปลี่ยนเป็น
`-e` ซึ่งสตาร์ตโปรแกรมตรง ๆ ด้วย argv ที่ให้ไป

## 11. 🪶 บล็อกเครื่องมือเบาลง 23% โดยที่โมเดลไม่ได้ถูกบอกน้อยลง

วัดจาก `tool_runs` จริง 68 เซสชัน 1,883 การเรียก ก่อนออกแบบอะไรทั้งสิ้น: เซสชันหนึ่งใช้เครื่องมือ
ต่างกัน **5 ตัว** (ค่ากลาง) เราส่งไป **38** และไม่มีเครื่องมือตัวไหนเลยที่ถูกใช้เกินครึ่งของเซสชัน
ตัวเลขนั้นฆ่าแผน "ส่ง 20 ตัวยอดนิยม" ทิ้ง เพราะไม่มีชุดหลักให้แบ่ง งานต่างกันที่**เรื่อง** ไม่ใช่
ที่ความเข้ม

เส้นที่ตัดถูกคืออายุการใช้งาน คำอธิบายเครื่องมือหนึ่งตัวแบกสามอย่างที่แยกกันได้:

| | ต้องอยู่ตอนไหน | น้ำหนัก (วัดบน browser) |
|---|---|---|
| มีอยู่จริง | ตลอดเวลา | ~10 token (1%) |
| เรียกยังไง | ตอนเรียก | ~70 token (9%) |
| **เมื่อไหร่ควรใช้** | **ครั้งเดียว** | **~686 token (90%)** |

ชั้นที่สามย้ายออกมาเป็น Guidance ส่งไปพร้อม**ผลลัพธ์แรก**ของเซสชัน ซึ่งเป็นการเรียกที่ยังไงก็ต้อง
เกิด จึงไม่เสียรอบเดินทางเพิ่มเลย ต่างจาก `defer_loading` ของ Anthropic หรือ progressive loading
ของ MCP ที่ต้องเสียหนึ่งรอบไปค้น schema (และแอปนี้ใช้ของเขาไม่ได้อยู่แล้ว เพราะไม่ได้ผูกกับ
ผู้ให้บริการเจ้าเดียว) และมันสอนตอนเรียกพลาดด้วย เพราะการเรียกที่พลาดคือตอนที่ขาดคำสอนพอดี

**ไม่แตะการให้สิทธิ์เลย** ทุกความสามารถยังอยู่ในบล็อกครบ ตลอดเวลา สิ่งที่ย้ายคือไบต์ ไม่ใช่สิทธิ์

สามตัวใหญ่ที่ย้ายจริงในรุ่นนี้:

```
browser   766 → 314 token   (คำสอน 830 กระจายเป็น 9 action)
task    1,568 → 730 token   (673 ของมันอยู่ในพารามิเตอร์ agent ตัวเดียว)
shell     854 → 498 token
```

รวมบล็อกทั้งหมด **10,224 → 7,848 token** ที่ค่าเริ่มต้น (8,578 ถ้าเปิดการมอบงาน)

สิ่งที่ **ไม่** ย้าย มีเหตุผลคนละข้อกัน และเป็นส่วนที่การย้ายจริงสอนเอง:

- **รายชื่อทีมใน `task`** ย่อได้แต่ต้องยังเลือกได้ เพราะการเลือกว่าจะมอบงานให้ใครเกิด**ก่อน**
  การมอบงานครั้งแรก คำสอนที่มาพร้อมผลลัพธ์แรกจึงมาช้าไปหนึ่งการตัดสินใจเสมอ ตัดได้เพราะ
  โปรไฟล์แบ่งครึ่งไว้ให้เองอยู่แล้ว: ก่อนขีดคือ worker นี้มีไว้ทำอะไร หลังขีดคือมันทำงานยังไง
- **ชื่อเชลล์ที่คำสั่งจะถูกรันด้วย** ไม่ใช่คำสอน มันคือข้อเท็จจริงว่าจะเขียนคำสั่ง "นี้" ยังไง และ
  มันเปลี่ยนทันทีที่ผู้ใช้ชี้เวิร์กสเปซไปที่ distro โมเดลที่ถูกบอกชื่อผิดจะเขียนผิดสำเนียงทุกคำสั่ง
  ตลอดเทิร์นโดยไม่มีทางรู้ตัวนอกจากพัง
- **ห้ามพิมพ์รหัสผ่านลงหน้าเว็บ** เป็นกฎความปลอดภัย และ Guidance อยู่ในสายข้อความที่บทสนทนา
  ยาวพอจะย่อแล้วหายเงียบ ๆ ยอมได้กับ "อ่านก่อนถ่ายรูป" ยอมไม่ได้กับข้อนี้
- **"เมื่อไหร่ควรหยิบ `wait`"** กลับด้านกับคำสอนอื่นทุกข้อ ความล้มเหลวที่มันกันคือการ**ไม่**เรียก
  `wait` เลยแล้วรายงานว่าไม่มีข้อมูล คำสอนที่ผูกกับการเรียก `wait` ครั้งแรกจึงไปถึงเฉพาะโมเดล
  ที่รู้อยู่แล้ว ย้ายไปขี่กับ `read` แทน: อ่านมาแล้วได้น้อยกว่าที่คาด นั่นมักไม่ใช่คำตอบ

`block_standard_test.go` บังคับเป็น**วงล้อกันถอย** ไม่ใช่เส้นตาย ตัวใหม่ต้องอยู่ใต้เพดาน ตัวเก่า
ลดได้ห้ามโต รายการยกเว้นจึงล้างตัวเอง (เทสต์ที่แดงตั้งแต่วันแรกคือเทสต์ที่คนลบทิ้ง)

## 12. 🎚️ การมอบงานเป็นสวิตช์ และมันส่งมาในสถานะปิด

77% ของเซสชันที่วัดได้ไม่เคยมอบงานเลย และการปิดอยู่ประหยัด **~730 token ตั้งแต่ข้อความแรก**
ปิดแล้ว**ไม่สร้าง tool เลย** ไม่ใช่สร้างแล้วปฏิเสธ ซึ่งจะยังเสีย 730 ไปบอกว่าไม่

สวิตช์มีสองชั้น: ตัวใหญ่ปิดการมอบงานทั้งหมด ตัวรายตัวเอาเอเจนคนหนึ่งออกจากรายชื่อที่ผู้ช่วยหลัก
เลือกได้ (~21 token ต่อคน) สวิตช์ใหญ่อยู่ใน**เมนูเลือกผู้ช่วยของช่องพิมพ์** ไม่ใช่ในหน้าตั้งค่า
อย่างเดียว เพราะความสามารถที่ปิดมาแต่แรกและซ่อนอยู่ในหน้าตั้งค่า คือความสามารถที่ถูกตัดทิ้ง

**การซ่อนชื่อไม่ใช่การห้าม** และนี่คือหนี้ที่เกือบสร้าง: ตัวกรองรายชื่อกับตัวสั่งงานเคยเป็นคนละทาง
โมเดลที่จำชื่อได้จากบริบทก่อนหน้าจึงสั่งงานเอเจนที่ถูกปิดได้เลย และในรายการเดียวกันนั้น เพดานของ
desk เป็นการห้ามจริง ส่วนสวิตช์ของผู้ใช้เป็นคำแนะนำ ของสองอย่างหน้าตาเหมือนกันอยู่ที่เดียวกัน
อันหนึ่งบังคับได้อันหนึ่งไม่ได้ ไม่ช้าก็เร็วจะมีคนพึ่งอันที่ผิด ตอนนี้รวมเป็นตัวเดียวที่ตอบคำถามเดียว
ว่าเซสชันนี้มอบงานให้คนนี้ได้ไหม และถ้าไม่ได้เพราะอะไร

ทุกที่บนหน้าจอเขียนว่า **"มอบงานให้"** ไม่ใช่ "เปิด/ปิด" เพราะสิ่งที่ปิดคือระยะเอื้อมของผู้ช่วยหลัก
ไม่ใช่ตัวเอเจน ผู้ใช้ยังเปิดแชทคุยเองได้ ยังพิมพ์ `@ชื่อ` ได้ แถวที่ถูกปิดจึง**จางลง ไม่ใช่หายไป**
เพราะแถวที่หายไปจะทำให้คนตามหาเอเจนที่ไม่ได้ไปไหน

และมิเตอร์บอกจำนวนโทเคนที่ประหยัดได้**วัดสดทุกครั้งที่เปิดหน้า** ไม่ใช่ค่าคงที่ วันที่มีคนรีด
คำอธิบายของ tool อื่นออก ตัวเลขนี้ต้องขยับตาม ไม่งั้นมันคือป้ายที่เคยจริง

## 13. 🌏 ภาษาที่สามเขียนได้แล้ว และจีนตัวย่อคือตัวพิสูจน์

คำถามว่า "เพิ่มภาษาอื่นยากไหม" มีคำตอบสองครึ่งที่ไม่เหมือนกันเลย ฝั่งหน้าจอพร้อมอยู่แล้ว
ฝั่งเครื่องยนต์ไม่พร้อม: คำที่ผู้ใช้อ่านถูกเก็บเป็น `pick(th, en)` **37 จุด** บวกสาขา `if english()`
อีก 4 ตัว ไม่มีอะไรผิดสำหรับสองภาษา มันผิดเรื่อง "มีได้กี่ภาษา" — **"สองพอดี" ถูกเขียนลงไปใน
ลายเซ็นระหว่างสตริงกับคนอ่าน** ต้นทุนของภาษาถัดไปจึงถูกจ่ายด้วยการแก้ที่ไม่เกี่ยวกับการแปลเลย

ตอนนี้เป็น map ที่บอกเรื่องสองภาษาที่มีอยู่ได้โดยไม่ตัดสินว่ามีได้กี่ภาษา `english() bool` หายไป
ทั้งตัว ซึ่งคือประเด็น: bool ตอบได้แค่สองเสมอ

สองกฎที่ผิดเฉพาะตอนมีเกินสองภาษา และผิดมาตั้งแต่ก่อนแปลคำแรก:

- **ตัดแท็กที่ขีดแรกเสมอ** ทำให้ `pt-BR` อ่านเป็น `pt` คือยื่นคำแปลโปรตุเกสยุโรปให้คนบราซิล
  แล้วดูเหมือนทำงานปกติทุกประการ ตอนนี้ลองแท็กเต็มก่อน แล้วค่อยครึ่งภาษา และค้นแบบไม่สน
  ตัวพิมพ์ เพราะคีย์ที่พลาดเพราะตัวพิมพ์จะไม่ error มันตกไปเจอคำแปลจริง แค่ผิดภาษา
- **ไทยเป็นตัวสำรอง** ซึ่งถูกโดยบังเอิญตอนที่ไทยเป็นภาษาเดียวที่เหลือ พอมีจีน คนที่เจอคีย์ที่ยัง
  ไม่ได้แปลจะได้ภาษาไทย ทั้งที่อังกฤษครบอยู่ข้าง ๆ ตกไปอังกฤษทั้งสองฝั่งแล้ว

ความครบแบ่งเป็นสองชั้น: บังคับครบด้วย type ได้เฉพาะภาษาที่เจ้าของเขียนเองและอ่านออก ถ้าบังคับ
กับภาษาที่อ่านไม่ออก มันแปลว่าเพิ่มปุ่มหนึ่งปุ่มแล้วบิลด์พังจนกว่าคำแปลจะมา ซึ่งไม่ทำให้ได้คำแปล
มันทำให้ได้ **placeholder ที่แปะเพื่อให้บิลด์เขียว และ placeholder คือคำโกหกที่ผ่าน type check**
เทสต์ตัวใหม่ตรวจทุกอย่างที่ตรวจได้โดยไม่ต้องอ่านภาษานั้น: ไม่มีคีย์แปลกปลอม ไม่มีค่าว่าง และ
`{ตัวแปร}` ต้องอยู่ครบ ข้อหลังคือเหตุผลที่ไฟล์นี้มีอยู่ คำแปลที่ทำ `{count}` หล่นจะไม่ error
มันจะแสดง *"คุณมี  รายการ"* ให้ผู้ใช้จริงเห็นตลอดไป

จีนตัวย่อได้ **423 จาก 1,032 คีย์** ที่เหลือตกไปอังกฤษตามที่ออกแบบ ฝั่งเครื่องยนต์แปลครบเฉพาะ
สิ่งที่คนยังไม่ต่อโมเดลจะได้อ่าน ฟอนต์ Noto Sans SC ผูกไว้ที่ `:lang(zh)` ไม่ใช่สแตกกลาง เพราะ
อักษรฮั่นใช้ร่วมกับญี่ปุ่นแต่วาดคนละแบบ ถ้าอยู่ในสแตกกลาง ข้อความญี่ปุ่นในแชทบน UI ภาษาไทยจะ
ถูกวาดด้วยรูปอักษรจีนแผ่นดินใหญ่เงียบ ๆ ซึ่งแย่กว่าที่ Windows ทำเอง

เก็บหนี้ก้อนเดียวกันไปด้วย: `index.html` เคยประกาศ `lang="th"` ตายตัวแล้วไม่เคยเปลี่ยนใจ ซึ่งเป็น
คำตอบที่ไปถึง hyphenation, spellcheck และเสียงที่ screen reader เลือก

## 14. 🪟 หน้าติดตั้งใหม่ห้าจอ และเจ้าที่ไม่มีคีย์ให้กรอก

จอเปิดโปรแกรมครั้งแรกเป็นหน้าเดียวในแอปที่ไม่มีผู้ใช้เก่า และเป็นหน้าเดียวที่**คนทำมองไม่เห็น**
เครื่องที่พัฒนามันมีคีย์อยู่แล้ว วิซาร์ดจึงปิดตัวเองทุกครั้ง ตอนนี้มีปุ่มใน **ตั้งค่า → ทั่วไป** ที่ตั้ง
ธงซึ่งชนะทุกทางลัด แล้วโหลดหน้าต่างใหม่ ล้างเฉพาะความจำเรื่องหน้าตา ไม่แตะคีย์ ประวัติแชท หรือ
ข้อความที่พิมพ์ค้างไว้

พอมองเห็นแล้วก็เจอว่ามันถามผิดคำถาม **"ต้องใช้คีย์" กับ "กรอกคีย์ได้" ถูกรวมเป็นข้อเดียว**
Codex ต้องมีเครดิตและไม่มีคีย์ให้กรอก เพราะมันคือซับสคริปชัน ChatGPT ส่วนคีย์ที่ใครจะวางได้
เป็นของ `api.openai.com` คนละโฮสต์ คนละบิล และ 401 แน่นอน หลังบ้านแยกไว้ตั้งแต่ต้นและหน้า
Settings ก็ใช้ถูก แต่อีกสามที่ไม่ได้ถาม — แบนเนอร์ใต้ช่องแชท วิซาร์ด และเมนูของ CLI ที่หนักสุด
เพราะมันวนถามไม่จบ (*"Missing API key. Try again."*) กับคีย์ที่ไม่มีวันมีอยู่จริง

จอถูกออกแบบใหม่ทั้งชุด จากกล่อง 560px ที่มี dropdown ของ OS สองอัน กลายเป็นห้าจอที่จัดกลาง
จอจริง ถามทีละคำถาม:

- **ภาษา**เป็นจอแรก และเป็นของสิ่งเดียวบนจอนั้น
- *"ตอนนี้คุณมีอะไรอยู่ในมือแล้วบ้าง"* ตอบเป็นภาษาคน ไม่ใช่ชื่อยี่ห้อ แล้วค่อยกางเฉพาะหมวดที่ตอบ
- **ธีมเลือกจากสีจริง** ซึ่งอ่านออกมาจาก `theme.css` ตอนเปิด ไม่ใช่ตารางสีชุดที่สอง
- ทุกจอมีปุ่มย้อนกลับ และ**ไม่มีทางข้าม**
- กดแล้ว**แถวที่กดเป็นคนรายงานเอง** ก่อนจอเลื่อนเอง จบด้วยจอ "พร้อมแล้ว" แทนที่จะหายไปเฉย ๆ

ค่าเริ่มต้นขนาดตัวอักษรเปลี่ยนตามที่วัดมาแล้ว: ทั้งระบบ **16px** แชท **14.5px**

## 15. 📏 มิเตอร์คอนเท็กซ์เลิกวาดตัวส่วนที่ไม่มีใครเคยส่งมา

มิเตอร์อ่านว่า `11.4k / 32.0k (36%)` **ครึ่งบนวัดจริง ครึ่งล่างถูกแต่งขึ้น** 32,000 ไม่เคยผ่านสาย
ไปหาใคร มันคืองบตัดประวัติ 128,000 หารสี่ ที่ถูกหยิบมาเป็น fallback ชั้นที่สาม

สองอย่างนี้เป็นคนละข้อเท็จจริงที่ใส่ป้ายเดียวกัน: **งบคือ Aetox เก็บประวัติได้เท่าไรก่อนย่อ
หน้าต่างคือโมเดลรับได้เท่าไร** ไม่มีอะไรเชื่อมกันเลย ประวัติของเครื่องตัวเองหักล้างเลขที่ตัวเองวาด:
รอบใหญ่สุดที่ codex รับไปคือ 43,434 token และมี 9 รอบที่เกินหน้าต่างที่วาดไว้ ขณะที่หน้าต่างจริง
ของรุ่นนั้นคือ 1,050,000

ตอนนี้ไม่รู้หน้าต่างคือ **0** และ 0 คือสิ่งที่ส่งออกไป UI ทิ้ง**ตัวส่วน** ไม่ทิ้งมิเตอร์ แล้วบอกขนาด
คำขอแทน เพราะการซ่อนมิเตอร์ไม่เคยเป็นทางเลือก แต่การอ้างถึงเลขที่ไม่มีใครมีก็ไม่ใช่

แล้วปัญหาถัดมาโผล่ทันทีที่หน้าต่างจริงมาถึง: **ตัวคูณ 4 เลิกเป็นของประดับ** วัดจากเซสชันจริงบน
เครื่องนี้ อัตราส่วนไบต์ต่อโทเคนอยู่ระหว่าง **3.45 ถึง 5.46** แกว่งตามสัดส่วนไทย/อังกฤษในเทิร์น
ซึ่งเป็นคุณสมบัติของ**งาน** ไม่ใช่ของโมเดล ที่ 3.45 งบ 4,200,000 ไบต์คือ 1.22M token บนหน้าต่าง
1,050,000 แปลว่างบที่อ่านว่ายังเหลือเยอะ เลยเส้นไปแล้ว และจูนเลขใหม่แก้ไม่ได้ เพราะค่าที่ต่ำพอ
จะปลอดภัยจะทิ้งหน้าต่างไปหนึ่งในสี่ทุกเซสชันที่ไม่ใช่เคสแย่สุด

เลยเลิกให้ค่าคงที่เป็นหลักประกัน **ผู้ให้บริการได้สิทธิ์ออกเสียง** ถ้าคำปฏิเสธที่กลับมาคือ "ยาวเกิน"
Aetox ย่อประวัติทันทีแล้วยิงรอบเดิมซ้ำ สูงสุดสองครั้ง ส่วน rate limit เงินหมด และ output ถูกตัด
ถูกกันออกโดยตั้งใจ สามอาการ สามวิธีแก้ การย่อประวัติไม่ช่วยสักอัน

ของเดิมไม่ได้แย่กว่านี้ **ของเดิมคือไม่มีอะไรเลย** ไม่มีส่วนไหนของเอนจินตอบสนองต่อการถูกปฏิเสธ
เพราะยาวเกิน เทิร์นตาย ประวัติที่ทำให้ตายยังอยู่ และทุกครั้งที่ผู้ใช้กดใหม่เองก็ตายเหมือนเดิม

## 16. 🌐 เบราว์เซอร์ของเอเจน: แท็บที่มันเปิดเอง ไม่ใช่แท็บที่บังเอิญอยู่หน้าสุด

สามบั๊กในสองวัน รากเดียวกันทั้งหมด คือกฎที่สำคัญที่สุดของระบบนี้อยู่ใน**คอมเมนต์** ไม่ได้อยู่ใน
ชนิดข้อมูล จึงไม่มีอะไรบังคับได้เลยนอกจากคนที่บังเอิญไปอ่านเจอ

- เป้าหมายของ `read`/`click`/`type` เคยเป็น "แท็บที่อยู่หน้าสุด" ซึ่งถูกเขียนทับทุกครั้งที่**ผู้ใช้**
  คลิกแท็บของตัวเอง ตอนนี้แท็บของเอเจนเป็นคนละชนิดกับแท็บทั่วไป
- การเปิดซ้ำเรียก navigate ตรงจากเธรดของ tool ซึ่ง WebView2 **ปฏิเสธทิ้งเงียบ ๆ** หน้าที่สอง
  เป็นต้นไปจึงตายที่ timeout 20 วินาทีมาตั้งแต่ 10 ส.ค. บรรทัดที่ทำให้เสียเวลาสองชั่วโมงตอนนี้
  **คอมไพล์ไม่ผ่าน** ไม่ใช่รอไปเจอตอนรัน
- คำบ่นของเอนจินเคยจบที่ log อย่างเดียว เอเจนเลยได้ *"page did not finish loading"* แล้วสรุปว่า
  เน็ตเสีย และบอกผู้ใช้แบบนั้น สามครั้ง

และอีกตัวที่เจอจาก log ของการรันจริง ไม่ได้เจอจากเทสต์: **`open` รายงานชื่อของหน้าที่เพิ่งจากมา**
สลับกันพอดีทั้งสองครั้งที่บันทึกไว้ การเดินทางถูกต้องเสมอ เบราว์เซอร์ไปที่ที่สั่งจริง ผิดแค่ประโยค
ที่ `open` รายงาน แต่โมเดลอ่านประโยคนั้น และหน้า "เว็บที่เอเจนเคยเปิด" ก็บันทึกชื่อผิดตามมาตั้งแต่
10 ส.ค. ตอนนี้การเริ่มเดินทางใหม่ล้างสิ่งที่เรารู้เกี่ยวกับหน้าเก่าทิ้ง และหน้าที่ไม่ส่งชื่อกลับมาเลย
เช่น PDF หรือรูป ใช้ URL ที่ขอไปเป็นชื่อ คือแลกชื่อผิดเป็นไม่มีชื่อ

ความสามารถที่เพิ่มทุกตัวลบทางตัน ไม่ใช่เพิ่มฟีเจอร์: `capture`, `tabs`, `wait`, `back`, `dialog`
และช่องที่อยู่ที่ค้นหาได้

## 17. 🛡️ ยามเชลล์: การเดาต้องมีหลักฐานก่อนจึงจะปฏิเสธได้

ตัวสแกนเส้นทางอ่าน `/…` ที่อยู่ในเครื่องหมายคำพูดเป็นเส้นทางสัมบูรณ์ ซึ่งปิด
`python3 -c "open('/etc/passwd')"` ได้จริง แต่มันก็ปฏิเสธวันไลเนอร์ POSIX ธรรมดาที่รูปทรง
บังเอิญเหมือนกันไปด้วย: `sed 's/^/n=/'`, `awk -F'/'`, `tr '/' '-'` และทุกฮีเอร์ด็อกที่เขียนชีแบง
มันล้มแบบปิด จึงกินเวลาทำงานแทนที่จะกินความปลอดภัย แต่กำแพงที่กั้นงานปกติก็ยังเป็นกำแพง

เส้นที่ลากใหม่: **เส้นทางที่คำสั่งเขียนมาเอง ตรวจตามที่เขียน · เส้นทางที่ไฟล์นี้เดาเอาจากรูปทรง
ต้องแสดงหลักฐานก่อนจึงจะปฏิเสธได้** ทุกโทเคนที่เชลล์จะส่งเป็นอาร์กิวเมนต์ถูกกั้นเหมือนเดิมทุก
ประการ ที่เปลี่ยนคือเฉพาะการอนุมาน หลักฐานที่ถูกที่สุดคือส่วนแรกของเส้นทางนั้นมีอยู่จริงไหม
(`/etc` `/home` `/mnt` `/tmp` มีทุกดิสโทร ส่วน `/n=` `/_` ไม่มีที่ไหน) และการค้นเกิดขึ้นเฉพาะ
ตอนที่การเดากำลังจะปฏิเสธ คำสั่งที่ผ่านจึงไม่เสียอะไรเลย

การล้มแบบปิดถูกยืนยันด้วยเทสต์ ไม่ใช่สมมติเอา: ระบบไฟล์ที่โปรเซสนี้เอื้อมไม่ถึงตอบว่า "คงคำ
ปฏิเสธไว้" เพราะ ***"ผมมองไม่เห็น" ต้องไม่ถูกอ่านเป็น "มันไม่มีอยู่"***

## 18. 📐 กฎที่ต้องอ่านก่อนทำจอ มีบ้านแล้ว

รอบรื้อจอติดตั้งได้กฎมาชุดหนึ่งที่ใช้ได้กับทุกจอ แต่มันไม่มีที่อยู่ `DECISIONS.md` เป็นบันทึกเรียง
ตามเวลา คนที่กำลังจะทำจอใหม่ไม่มีทางไล่อ่าน 3,700 บรรทัดเพื่อหาว่ากดปุ่มแล้วควรยุบกี่เปอร์เซ็นต์

`DESIGN.md` จึงเกิดขึ้น และเป็นไฟล์ที่**ชนะ**เมื่อเอกสารสามตัวขัดกันเรื่องหน้าตา กฎทุกข้อ
ในนั้นมีจอจริงที่พังมาแล้วอยู่ข้างหลัง: ความว่างที่ไม่สมมาตรคือบั๊ก ไม่ใช่รสนิยม และวัดได้ ·
ตัวเลือกเกินเจ็ดต้องมีคำถามคั่นก่อน · **ยืมโครงได้ ห้ามยืมเครื่องประดับ** · ทุกครั้งที่เอาปุ่มข้าม
ออก ต้องระบุให้ได้ว่าประตูไหนเปิดได้เสมอ · เทสต์ต้องล็อกสิ่งที่**ห้าม**มี ไม่ใช่แค่สิ่งที่ต้องมี

ส่วนที่ไม่ใช่แค่เอกสาร: จังหวะการเคลื่อนไหวถูกรวบเป็นโทเคนใน CSS แล้ว `DESIGN.md` เรียกชื่อ
โทเคน ไม่เขียนมิลลิวินาที เพราะตัวเลขในเอกสารคือที่ที่สองที่ตอบคำถามเดียวกัน และวันหนึ่งมันจะ
ไม่ตรงกับ CSS

## 19. 🧩 เล็ก ๆ แต่รู้สึกได้

- **สกิลของผู้ช่วยที่โมเดลมองไม่เห็น** เอกสารถูกลงทะเบียน เรียกใช้ได้ด้วยชื่อ และไม่มีชื่ออยู่ที่ไหน
  ที่โมเดลมองเห็น ตลอดสองรุ่นที่ผ่านมา — `skills_list` ที่ผู้ช่วยได้รับสแกนแต่ชั้นของเครื่อง
  ไม่เคยเห็นชั้นของตัวเอง ตอนนี้ประตูชี้ไปที่ชั้นของมันด้วย
- **เมนูเหนือช่องพิมพ์วัดกับแพนที่มันเปิดอยู่** ไม่ใช่กับหน้าจอ (หน้าต่าง 1600px ที่แบ่งให้แชท
  300px คือกรณีที่ media query ตอบผิดทุกครั้ง) และชิปเหนือช่องพิมพ์ตัดบรรทัดแทนที่จะวิ่งเลย
  ขอบ — ชิปเทอร์มินัลเคยอ่านว่า "Windo"
- **เริ่มงานในโปรเจกต์ได้จากแถวของโปรเจกต์เอง** ปุ่ม + บนแถว แทนการคลิกชื่อซึ่งทุกคนอ่านว่า
  "เปิดดู"
- **ระยะห่างใต้คำตอบ** แถวเวลา/ปุ่มเคยชิดคำตอบมากกว่าที่บรรทัดในคำตอบชิดกันเอง จึงอ่านเป็น
  บรรทัดหนึ่งของคำตอบ
- **ลายน้ำหลังหน้าแชทเปล่าถูกตัดหัวท้ายข้างละ 8px** เป็นรอยตัดตรง ๆ ไม่ใช่การไล่จาง มาร์ค
  ตั้งขนาดจาก**ความสูงหน้าต่าง** แล้วไปยืนอยู่ในกรอบที่เตี้ยกว่าไม่กี่พิกเซล ต่างกันมาตลอด
  แต่ต่างน้อยพอที่ส่วนเกินจะไม่ถึงหนึ่งพิกเซล จนวันที่ขนาดตัวอักษรทั้งระบบขยับเป็น 16px
  มาสก์มีไว้ไล่จาง กล่องไม่มีสิทธิ์ตัด
- **แท็บว่างของเบราว์เซอร์** เคยกองอยู่บนซ้าย ปล่อยครึ่งล่างตาย มีคำสอนสองประโยคแข่งกันเอง
  และหัวข้อว่า "แท็บนี้ยังว่าง" ทั้งที่ในหน้ามีประวัติ 24 รายการรออยู่ ตอนนี้การ์ดจัดกลางทั้งสอง
  แกน หัวข้อบอกว่ามันเป็นอะไรแทนที่จะบอกว่ามันไม่มีอะไร และยืนบนพื้นเดียวกับห้องว่างอื่นในแอป
- **เรซในเทสต์เบนช์** ที่จ็อบลินุกซ์จับได้และวินโดวส์ไม่เห็น โค้ดจริงไม่เกี่ยว ตัวเทสต์เองที่ไม่มี
  กุญแจ และเครื่องเจ้าของไม่มีคอมไพเลอร์ C จ็อบลินุกซ์จึงเป็นที่เดียวที่เรื่องนี้ถูกตรวจจริง

## ⚠️ สิ่งที่รุ่นนี้ยังไม่ได้ติ๊ก

**ตัวสรุปยังเสนอลงส่วนกลางที่เดียว** ชั้นความจำรายโปรเจกต์เขียนได้แล้วโดยเอเจน แต่ตัวสรุปที่อ่าน
`tool_runs` ยังไม่เล็งข้อเสนอไปที่โปรเจกต์ ทั้งที่ข้อมูลที่ต้องใช้อยู่ในมือมันแล้ว

**เอเจนยังไม่เขียน `AETOX.md`** และไม่ควรเขียนวันนี้ การเสนอบรรทัดให้ไฟล์ของทีมเป็นการกระทำ
คนละอย่างกับการจำ มันต้องใช้สิทธิ์เขียนรีโปและ diff ที่คุณอ่าน

**หน้าต่างยังไม่จำว่าคุณวางมันไว้ไหน** ทุกครั้งที่เปิดยังเป็นกึ่งกลางที่ขนาดที่คำนวณได้

**จีนตัวย่อยังไม่ครบ** 423 จาก 1,032 คีย์ ที่เหลือตกไปอังกฤษตามที่ออกแบบ ไม่ใช่ค้างเป็นช่องว่าง
และสิ่งที่รุ่นนี้แก้จริงคือ *"เพิ่มภาษาที่สามได้"* ไม่ใช่ *"ภาษาที่สามครบแล้ว"*

**อัตราส่วนไบต์ต่อโทเคนยังเป็นค่าคงที่** งบตัดประวัติยังคูณสี่เหมือนเดิม สิ่งที่เปลี่ยนคือมันเลิกเป็น
หลักประกันตัวเดียว เพราะคำปฏิเสธของผู้ให้บริการมาเป็นตาข่ายรับแล้ว การ calibrate จาก
`token_usage` จริงจึงกลายเป็นเรื่องประสิทธิภาพ ไม่ใช่ความถูกต้อง และยังไม่ได้ทำ

**ผู้ช่วยที่ยิงคำสั่งไม่ซ้ำยังไม่มีอะไรหยุด** ตัวจับลูปซ้ำจับได้เฉพาะ call ที่ซ้ำเป๊ะ ๆ ปุ่ม Stop ยังเป็น
เบรกเดียว (`DECISIONS §110`) — และ run ที่ประกาศไว้ก็ไม่มีเพดานโทเคนโดยตั้งใจ

**BENCHMARK ยังค้าง** รอบวัดเต็มครั้งสุดท้ายคือ 2026-07-27 บน v0.9.2 ข้อ *"ตัวเลขที่เผยแพร่
ทุกตัวตรงกับของจริง"* จึงยังเปิดค้างอยู่ในวันที่บิลด์นี้ออก

**Linux / macOS ยังเป็นพอร์ตที่ทำอยู่** CI รันทั้งสองตัวและเก็บ log ไว้ให้ดู แต่ไม่ให้มันตัดสินผลรวม
เพราะ Windows คือของที่ปล่อยจริง

**การเซ็นโค้ด** ยังไม่มี (ตัวติดตั้งยังขึ้น SmartScreen) — แต่ *ตัวรุ่นที่ปล่อย* เซ็นด้วย ed25519
ตั้งแต่ v0.9.4 และตัวอัปเดตปฏิเสธการอัปเดตที่ลายเซ็นไม่ผ่าน ไม่ใช่ยอมผ่านไป
