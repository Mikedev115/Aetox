This folder is a web project: the page is src/index.html, served by vite with src/ as the root (see vite.config.js). Tasks come one at a time; do each one so that the earlier ones keep working.

Task 1: Add svg element (class `system`, viewBox is (0 0 160 160)) in `.root`. `.system` fill the whole space of `.root`. Add defs element (id `systemDefs`) and g element (id `systemRoot`) in `.system`.