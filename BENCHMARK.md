# BENCHMARK — วัด Aetox เทียบกับเครื่องมือ AI ตัวอื่น

> เอกสารบันทึกผล ไม่ใช่ข้อเสนอ · เริ่ม 2026-07-27
>
> **กฎข้อเดียวของเอกสารนี้: ตัวเลขที่ยังไม่ผ่านกติกาข้างล่าง ห้ามขึ้น [README.md](README.md) หรือ [docs/index.html](docs/index.html)**
> จุดขายทั้งหน้าเว็บคือ "วัดจริง ไม่ใช่คำโฆษณา" — ตัวเลขเกินจริงตัวเดียวลากตัวที่เหลือลงไปด้วยทั้งหมด

รันซ้ำได้ด้วย [bench.ps1](bench.ps1) ที่อยู่ข้าง ๆ กัน · วัดด้วยมือแล้วจดเองไม่นับ เพราะพิสูจน์สภาพตอนวัดไม่ได้

---

## 1. กติกา

ตัวเลขจะเทียบกันได้ก็ต่อเมื่อทุกตัวเจอเงื่อนไขเดียวกัน ข้อที่พังบ่อยที่สุดคือข้อ 3 กับข้อ 9

| # | กติกา | ทำไม |
|---|---|---|
| 1 | เครื่องเดียวกัน รอบเดียวกัน | เอาตัวเลขคนละวันมาปนกันคือคนละเครื่องในทางปฏิบัติ — โหลดเครื่องต่างกัน |
| 2 | ปิดทุกโปรแกรมที่ไม่เกี่ยวก่อนเริ่ม รวมเบราว์เซอร์ | RAM ที่เครื่องเหลืออยู่มีผลต่อ working set โดยตรง |
| 3 | **สภาพเดียวกันทุกตัว** — เปิดเปล่า ไม่เปิดโปรเจกต์ ไม่ล็อกอิน ไม่ลงส่วนเสริม | เทียบแอปเปิดเปล่ากับแอปที่รันงานอยู่ = ตัวเลขปลอม แม้ทั้งคู่จะวัดถูกต้อง |
| 4 | 5 รอบ · ทิ้งรอบแรก · ที่เหลือเอา median | รอบแรกคือ cold รอบถัดไปคือสิ่งที่ผู้ใช้เจอทุกวัน ค่ากลางกันตัวหลุด |
| 5 | รอให้นิ่ง 60 วิ ก่อนอ่าน RAM | Chromium/Electron โหลดต่อหลังหน้าต่างขึ้นแล้ว วัดเร็วไปได้ตัวเลขต่ำเกินจริง |
| 6 | นับทุก process ของแอปนั้น | แอปยุคนี้ไม่มีตัวไหนเป็น process เดียว ดูแต่ตัวแม่คือดูไม่ครบ |
| 7 | ใช้ **private bytes** ไม่ใช่ working set | working set ขึ้นลงตาม RAM ที่เหลือของเครื่อง เทียบข้ามรอบไม่ได้ |
| 8 | จดเวอร์ชันทุกตัว | อีก 3 เดือนตัวเลขชุดนี้ไม่มีความหมายถ้าไม่รู้ว่าวัดเวอร์ชันไหน |
| 9 | **นับเฉพาะสิ่งที่ผู้ใช้ได้ลงไปจริง** — ไม่นับแคช โปรไฟล์ ไฟล์ dev ค้าง | ดูข้อ 5 ข้างล่าง ข้อนี้ทำตัวเลขเพี้ยนได้เป็นเท่าตัว ทั้งฝั่งเราและฝั่งเขา |

**เรื่อง cold start:** "เปิดครั้งแรกจริง ๆ" ต้องรีบูตก่อนแล้ววัดรอบเดียว ถ้าเครื่องเปิดมานานแล้ว ไฟล์ของแอปค้างอยู่ใน file cache ของ Windows รอบ "แรก" จะเร็วกว่าความจริง — `bench.ps1 -Start` เตือนให้เองเมื่อเจอกรณีนี้

---

## 2. เครื่องที่ใช้วัด

| | |
|---|---|
| CPU | Intel Core i7-13620H · 10 คอร์ / 16 เธรด |
| RAM | 32 GB |
| ดิสก์ | GIGABYTE AG4512G-SI B10 (NVMe) · C: 459 GB |
| OS | Windows 11 Home Single Language · build 26200 |

เครื่องเดียว ยังไม่ได้ทวนบนเครื่องสเปกต่ำ — ผลบนเครื่องช้ากว่านี้อาจต่างออกไป โดยเฉพาะเวลาเปิด

---

## 3. คู่แข่งที่เอามาเทียบ

ทุกตัวในตารางนี้**ลงอยู่บนเครื่องเดียวกัน** ไม่มีตัวไหนเอาตัวเลขมาจากเว็บ

| กลุ่ม | ใครบ้าง | เทียบกับ Aetox ได้แค่ไหน |
|---|---|---|
| **ผู้ช่วย AI เดสก์ท็อป** | Comet (Perplexity) · Qwen | ใกล้ที่สุด — เป็นแอปมีหน้าต่าง คุยกับ AI ได้ ไม่ใช่ IDE |
| **IDE ที่มี AI ในตัว** | Cursor · Windsurf · Antigravity (Google) · Zed · VS Code | คนละงานกับ Aetox แต่แย่ง RAM และพื้นที่บนเครื่องเดียวกัน |
| **พิมพ์คำสั่ง (CLI)** | Claude Code · Codex · OpenCode · Copilot CLI | ไม่มีหน้าต่าง เทียบเวลาเปิดไม่ได้ · เทียบขนาดได้ และนั่นคือหมัดหนัก |
| **ตัวรันโมเดล** | Ollama | คนละประเภท เก็บไว้อ้างอิงเฉย ๆ **ห้ามเอาไปเทียบตรง ๆ** |

**Manus** ยังไม่มีอะไรลงเครื่อง — เป็นบริการบนเว็บล้วน ไม่มีตัวเลขให้วัด ไม่ต้องใส่ในตารางเปรียบเทียบ

---

## 4. ผล 1 — พื้นที่ในเครื่อง ✅ วัดครบแล้ว

วัด 2026-07-27 · ครบทุกตัวในรอบเดียว ไม่ต้องเปิดโปรแกรม จึงไม่มีปัญหาเรื่องสภาพ

| เครื่องมือ | กลุ่ม | ขนาด | เท่าของ Aetox |
|---|---|---|---|
| **Aetox** | ผู้ช่วย AI | **33 MB** | — |
| Copilot CLI | CLI | 137 MB | 4× |
| Claude Code | CLI | 236 MB | 7× |
| Zed | IDE | 419 MB | 13× |
| Qwen | ผู้ช่วย AI | 457 MB | 14× |
| OpenCode | CLI | 498 MB | 15× |
| Comet (Perplexity) | ผู้ช่วย AI | 501 MB | 15× |
| Codex (npm) | CLI | 698 MB | 21× |
| Cursor | IDE | 874 MB | 26× |
| Windsurf | IDE | 940 MB | 28× |
| Antigravity (Google) | IDE | 1,030 MB | 31× |
| Codex (ตัวติดตั้งเนทีฟ) | CLI | 1,123 MB | 34× |
| VS Code | IDE | 1,171 MB | 35× |
| Ollama _(คนละประเภท)_ | รันโมเดล | 2,812 MB | 85× |

ตัวติดตั้งที่ผู้ใช้โหลด **12.6 MB** · ตัวโปรแกรมที่ลงแล้ว **32.6 MB** (`aetox.exe` ไฟล์เดียวจบ)

**Codex ลงได้สองทาง ขนาดต่างกันเกือบเท่าตัว** — ผ่าน npm 698 MB, ตัวติดตั้งเนทีฟ 1,123 MB
เวลาอ้างอิงต้องบอกด้วยว่าอันไหน เพราะบนเครื่องนี้ `codex` ที่อยู่ใน PATH ชี้ไปที่ตัว npm

**เหตุผลที่เล็กได้ขนาดนี้:** Go คอมไพล์เป็นไฟล์เดียว ไม่ต้องแบก Node.js runtime มาด้วย และหน้าจอใช้ WebView2 ที่ Windows มีอยู่แล้ว ไม่ได้บรรจุ Chromium สำเนาของตัวเองมาเหมือน Electron

---

## 5. บทเรียนจากรอบวัดแรก — ของที่ห้ามนับ

รอบแรกได้ตัวเลขผิดทั้งฝั่งเราและฝั่งคู่แข่ง เพราะไปนับทั้งโฟลเดอร์แบบไม่ดูข้างใน จดไว้กันพลาดซ้ำ

| เครื่องมือ | นับทั้งโฟลเดอร์ได้ | ของจริง | ที่ปนเข้ามา |
|---|---|---|---|
| **Aetox** | 93 MB | **33 MB** | `aetox-dev.exe~` 47.5 MB (ไฟล์ dev ค้าง) + ตัวติดตั้ง 12.6 MB |
| **Comet** | 2,001 MB | **501 MB** | `Comet\User Data` 1,283 MB — โปรไฟล์กับแคชเบราว์เซอร์ |
| **Claude Code** | 301 MB | **236 MB** | `uv.exe` 64.6 MB กับ python shim ของเจ้าของเครื่องที่บังเอิญอยู่ใน `.local\bin` |

ถ้าปล่อยไว้ Aetox จะดูใหญ่กว่าความจริง 3 เท่า และ Comet จะดูใหญ่กว่าความจริง 4 เท่า — **ผิดทั้งสองทาง**
ตัวที่เข้าข้างเราคือตัวที่อันตรายที่สุด เพราะไม่มีใครอยากตรวจตัวเลขที่ทำให้ตัวเองดูดี

**กติกาที่ได้จากบทเรียนนี้ (ข้อ 9):** `Dir` ในสคริปต์ต้องชี้ไปที่สิ่งที่ผู้ใช้ได้ลงจริงเท่านั้น — ชี้ไฟล์เดียวได้ถ้าแอปเป็นไฟล์เดียว และห้ามชี้โฟลเดอร์ที่มีข้อมูลผู้ใช้ปน

### เรื่องเดียวกันเกิดกับ RAM ด้วย

รอบแรกของ `-Snapshot` รายงานว่า Aetox กิน **904 MB / 12 process** ทั้งที่ตอนนั้น Aetox ไม่ได้เปิดอยู่เลยสักตัว

สาเหตุ: Aetox ใช้ WebView2 ซึ่งทุกแอปบนเครื่องที่ใช้ WebView2 ต่างก็รัน `msedgewebview2.exe` ชื่อเดียวกันหมด นับตามชื่อ process เฉย ๆ เลยกวาด RAM ของแอปอื่นมาเป็นของเรา

แก้ด้วยการกรอง command line (`CmdMatch` ใน `$Apps`) — โหมด `-Start` ไม่เจอปัญหานี้เพราะมันเทียบรายชื่อ process ก่อน/หลังเปิดอยู่แล้ว แต่ `-Snapshot` กับ `-Soak` ไม่มี baseline จึงต้องพึ่งการกรองนี้

> จุดร่วมของบทเรียนทั้งสองข้อ: **นับของที่ไม่ใช่ของตัวเอง** ไม่ว่าจะเข้าข้างเราหรือไม่ ก็คือตัวเลขผิดเหมือนกัน

---

## 6. ผล 2 — RAM ตอนทำงาน ⚠️ วัดได้ไม่ครบ

**ตัวเลขที่สำคัญที่สุดในเอกสารนี้** ผู้ใช้เจอมันตลอดเวลาที่เปิดโปรแกรมไว้ ต่างจากขนาดติดตั้งที่เจอครั้งเดียวตอนลง

### ที่ผ่านกติกาแล้ว

| เครื่องมือ | private | process | สภาพตอนวัด | ขึ้นเว็บได้ |
|---|---|---|---|---|
| **Aetox** | **252 MB** | 7 | เปิดเปล่า นิ่งแล้ว | ✅ |
| Zed | 471 MB | 4 | เปิดเปล่า นิ่งแล้ว | ✅ |

Aetox กิน RAM ราวครึ่งเดียวของ Zed ทั้งที่ Zed เป็นแอป Rust เนทีฟที่ขึ้นชื่อเรื่องเบา

### ที่ยังไม่ผ่านกติกา — ห้ามขึ้นเว็บจนกว่าจะวัดใหม่

| เครื่องมือ | private | process | สภาพตอนวัด | ปัญหา |
|---|---|---|---|---|
| Cursor | 1,750 MB | 10 | เปิดใช้งานจริง ~6 นาที พร้อมส่วนเสริมของเจ้าของเครื่อง | ผิดกติกาข้อ 3 |
| VS Code | 4,814 MB | 24 | กำลังรัน Claude Code session อยู่ | ผิดกติกาข้อ 3 หนักสุด |
| Claude Code | 4,497 MB | 18 | กำลังทำงานจริงระหว่าง session เดียวกันนั้น | ผิดกติกาข้อ 3 · เป็น CLI ไม่มีสภาพ "เปิดเปล่า" |

> ตัวเลข 4,814 MB ของ VS Code คือ VS Code ที่กำลังแบก AI agent session ของเราเองอยู่ ไม่ใช่ VS Code เปิดเปล่า
> ถ้าเอาไปเทียบกับ Aetox เปิดเปล่าจะได้ "เบากว่า 19 เท่า" ซึ่งอ่านแล้วดูดีแต่ไม่จริง

### ยังต้องวัด

- [ ] Cursor · Windsurf · Antigravity · VS Code · Comet · Qwen — เปิดเปล่าทั้งหมด รอบเดียวกัน
- [ ] **Aetox ตอนทำงานจริง** — คุยไปสัก 20 เทิร์น เปิดแท็บเบราว์เซอร์ ใช้เครื่องมือครบ แล้ววัดใหม่
      ข้อนี้สำคัญที่สุด: ตอนนี้เรามีแค่ตัวเลขเปิดเปล่าของตัวเอง ถ้าจะเทียบกับตัวอื่น "ตอนใช้งานจริง" ก็ต้องมีของเราด้วย

---

## 7. ผล 3 — ความเร็วตอนเปิด ⚠️ วัดได้ 2 ตัว

| เครื่องมือ | เปิดครั้งแรก (cold) | เปิดครั้งถัดไป | สภาพตอนวัด | ขึ้นเว็บได้ |
|---|---|---|---|---|
| **Aetox** | **1.77 วิ** | **0.53 วิ** | เปิดเปล่า · median 5 รอบ | ✅ |
| Zed | 2.12 วิ | 0.53 วิ | เปิดเปล่า · median 5 รอบ | ✅ |
| Cursor | — | — | เปิดจากสคริปต์ไม่ได้ | ⬜ |
| Windsurf · Antigravity · VS Code · Comet · Qwen | — | — | ยังไม่ได้วัด | ⬜ |

จับเวลาถึงตอน **หน้าต่างโผล่บนจอ** (`MainWindowHandle != 0`) ไม่ใช่ตอน process เกิด — เป็นตัวแทนของ "เห็นแล้วกดได้" ที่ใกล้ความจริงที่สุดเท่าที่วัดอัตโนมัติได้

**ทำไม Cursor ถึงจับเวลาไม่ได้ในรอบแรก:** `Cursor.exe` ที่เรียกทำหน้าที่เป็น launcher มันโยนงานให้ process อื่นแล้วจบตัวเองทันที สคริปต์เลยไม่มี handle ให้ตาม — Windsurf กับ Antigravity เป็น VS Code fork เหมือนกัน เจอปัญหาเดียวกัน

**แก้ในสคริปต์แล้ว (ยังไม่ได้ทดสอบกับของจริง):** `Wait-Window` มองหาหน้าต่างของ process ที่เกิดใหม่ทุกตัว ไม่ใช่แค่ตัวที่สั่งเปิด และ `Stop-App` ปิดแบบกดกากบาทก่อนค่อยบังคับปิด เพราะการฆ่าดิบ ๆ ทุกรอบจะทำให้รอบถัดไปเจอหน้าต่าง "กู้คืนเซสชันที่ปิดผิดปกติ" แล้วเวลาเปิดเพี้ยนทั้งชุด — ถ้ารอบวัดจริงยังจับไม่ได้ ให้จดไว้ตรงนี้ว่าเป็นข้อจำกัดถาวร

**ทำไม cold ของ Aetox ช้ากว่า warm 3 เท่า:** รอบแรก Windows ต้องอ่าน binary กับ WebView2 runtime จากดิสก์จริง รอบถัดไปอยู่ใน cache แล้ว — 0.53 วิ คือสิ่งที่ผู้ใช้เจอแทบทุกครั้ง ส่วน 1.77 วิ เจอครั้งเดียวหลังรีบูต

---

## 8. ผล 4 — ความเสถียร

### ตอน build ✅

| | ผล |
|---|---|
| `go test ./...` | **745 / 745** · 24 package · 0 fail |
| เทสต์ฝั่งหน้าจอ | **83 / 83** |
| ตรวจชนิดข้อมูล | Go + TypeScript + `svelte-check` ทั้งสองฝั่ง |

### ตอนใช้งานยาว ๆ ⬜ ยังไม่ได้วัด

ช่องว่างใหญ่ที่สุดของเอกสารนี้ตอนนี้ เทสต์ 745 ตัวบอกได้แค่ว่าโค้ดถูกตอนคอมไพล์ ไม่ได้บอกว่าเปิดทิ้งไว้ 8 ชั่วโมงแล้วยังดีอยู่ไหม

รันด้วย `.\bench.ps1 -Soak -Hours 8` แล้วดู 3 อย่าง:

| ดูอะไร | เกณฑ์ตก |
|---|---|
| RAM โตขึ้นเรื่อย ๆ ไม่ลงเลย | โตเกิน ~10% ต่อชั่วโมงแบบไม่กลับลง = น่าจะรั่ว |
| process หายไประหว่างทาง | แอปดับเอง = ตกทันที |
| หน่วงลงเมื่อแชทยาวขึ้น | จดเวลาตอบของเทิร์นที่ 1 เทียบเทิร์นที่ 50 |

- [ ] Aetox soak 8 ชม.
- [ ] Cursor soak 8 ชม. (สภาพเดียวกัน)

---

## 9. คู่แข่งที่ยังไม่ได้ลง

**ไม่เอาตัวเลขจากเว็บมาใส่ตาราง** — ขนาดตัวติดตั้งที่เขาประกาศ ไม่เท่ากับขนาดหลังลงจริง (Aetox เองต่างกัน 12.6 vs 32.6 MB) เอามาปนกับตัวเลขที่วัดเองจะพังทั้งตาราง ตัวไหนอยากได้ก็ลงแล้ววัด ใช้เวลาเพิ่มตัวละไม่กี่นาที

| เครื่องมือ | กลุ่ม | ลงยังไง |
|---|---|---|
| Trae (ByteDance) | IDE | โหลดจากเว็บ |
| Kiro (AWS) | IDE | โหลดจากเว็บ |
| Void | IDE | โหลดจากเว็บ |
| JetBrains + AI Assistant | IDE | Toolbox |
| Warp | เทอร์มินัล AI | โหลดจากเว็บ |
| LM Studio | ผู้ช่วย AI | เครื่องนี้เหลือแต่โฟลเดอร์ updater — ลงใหม่ก่อนถึงจะวัดได้ |
| Jan · Msty · Cherry Studio · AnythingLLM | ผู้ช่วย AI | โหลดจากเว็บ — กลุ่มนี้ใกล้ Aetox ที่สุด ควรลงก่อนเพื่อน |
| Gemini CLI | CLI | `npm i -g @google/gemini-cli` |
| Aider | CLI | `uv tool install aider-chat` |
| Goose (Block) | CLI | โหลดจากเว็บ |
| Amp (Sourcegraph) | CLI | `npm i -g @sourcegraph/amp` |
| Crush (Charm) | CLI | `npm i -g @charmland/crush` |

เพิ่มเข้าไปวัดได้ด้วยการเติมแถวเดียวใน `$Apps` ที่หัวไฟล์ [bench.ps1](bench.ps1) — ตัวไหนไม่มีบนเครื่อง สคริปต์ข้ามให้เอง

---

## 10. ที่วัดไม่ได้ และเพราะอะไร

บอกไว้ให้ครบ เพราะการยอมรับว่าอะไรวัดไม่ได้คือสิ่งที่ทำให้ตัวเลขที่เหลือน่าเชื่อ

| เครื่องมือ | วัดไม่ได้เพราะ |
|---|---|
| Claude Code · Codex · OpenCode · Copilot CLI | ไม่มีหน้าต่างแอป จับเวลาเปิดไม่ได้ และไม่มีสภาพ "เปิดเปล่า" ให้เทียบ RAM — เทียบได้แค่ขนาด |
| Manus | บริการบนเว็บล้วน ไม่มีอะไรลงเครื่อง — **ไม่ต้องเอาเข้าตารางเลย** ใส่ไปก็เป็นแถวว่าง |
| VS Code (เวลาเปิด/RAM) | ยังไม่ได้วัด ต้องปิด session ที่กำลังทำงานอยู่ก่อน |
| Cursor (เวลาเปิด) | launcher จบตัวเองทันที สคริปต์ตามไม่ทัน — ดูข้อ 7 |

> **ต้องพูดตรง ๆ เรื่อง RAM:** WebView2 ก็คือ Chromium เหมือนกัน ที่เราใช้ RAM น้อยกว่าไม่ใช่เพราะเอนจินวาดหน้าจอวิเศษกว่า
> แต่เพราะมันเป็นส่วนกลางของ OS ที่แชร์กับแอปอื่น ไม่ใช่สำเนาต่อแอป และเพราะเราไม่ได้ยัดทุกอย่างไว้ในโปรแกรมเดียว
> ข้อนี้ต้องอยู่รอดทุกครั้งที่แก้หน้าเว็บ

---

## 11. วิธีรันซ้ำ

```powershell
.\bench.ps1 -Disk                   # ขนาดในเครื่อง — รันได้ทุกเมื่อ ไม่ต้องปิดอะไร
.\bench.ps1 -Snapshot               # อ่าน RAM ของตัวที่เปิดค้างอยู่ ไม่แตะอะไร
.\bench.ps1 -Start                  # เวลาเปิด + RAM — ต้องปิดตัวที่จะวัดก่อน
.\bench.ps1 -Start -App Aetox       # เฉพาะตัวเดียว
.\bench.ps1 -Soak -Hours 8          # เปิดทิ้งไว้แล้วเก็บ RAM ทุก 5 นาที → logs/soak-*.csv
```

**ลำดับที่ถูกต้องสำหรับรอบวัดใหญ่:**

1. รีบูต — ให้ทุกตัวเจอ file cache สภาพเดียวกัน
2. ไม่เปิดอะไรเลย → `.\bench.ps1 -Start` (ได้ cold ของทุกตัวในรอบเดียว)
3. `.\bench.ps1 -Disk`
4. เปิดตัวที่จะ soak แล้ว `.\bench.ps1 -Soak -Hours 8` ทิ้งไว้ข้ามคืน
5. เอาผลมาลงเอกสารนี้ พร้อม**สภาพตอนวัด**ทุกแถว แล้วค่อยยกขึ้นหน้าเว็บ

---

## 12. ประวัติการวัด

| วันที่ | วัดอะไร | ผล |
|---|---|---|
| 2026-07-27 | ขนาดในเครื่อง 14 ตัว (รอบแก้แล้ว) | ✅ ผ่านกติกา — ตารางข้อ 4 |
| 2026-07-27 | ขนาดในเครื่อง รอบแรก | ❌ นับของปนเข้ามา ดูข้อ 5 — ยกเลิก |
| 2026-07-27 | Aetox v0.5.0 + Zed เปิดเปล่า (เวลาเปิด + RAM) | ✅ ผ่านกติกา — ขึ้นหน้าเว็บแล้ว |
| 2026-07-27 | Cursor · VS Code · Claude Code ตอนใช้งานจริง | ⚠️ ไม่ผ่านกติกาข้อ 3 · เก็บไว้อ้างอิงเท่านั้น |

**ที่ต้องแก้บนหน้าเว็บรอบหน้า:** Cursor โตจาก 824 → **874 MB** (อัปเดตเวอร์ชันระหว่างทาง) ทำให้ "เล็กกว่า Cursor 25 เท่า" กลายเป็น **26 เท่า** — ส่วน VS Code 35 เท่ายังตรงเดิม
