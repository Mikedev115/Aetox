# BENCHMARK — วัด Aetox เทียบกับเครื่องมือ AI ตัวอื่น

> เอกสารบันทึกผล ไม่ใช่ข้อเสนอ · เริ่ม 2026-07-27
>
> **กฎข้อเดียวของเอกสารนี้: ตัวเลขที่ยังไม่ผ่านกติกาข้างล่าง ห้ามขึ้น [README.md](README.md) หรือ [docs/index.html](docs/index.html)**
> จุดขายทั้งหน้าเว็บคือ "วัดจริง ไม่ใช่คำโฆษณา" — ตัวเลขเกินจริงตัวเดียวลากตัวที่เหลือลงไปด้วยทั้งหมด

รันซ้ำได้ด้วย [bench.ps1](bench.ps1) ที่อยู่ข้าง ๆ กัน · วัดด้วยมือแล้วจดเองไม่นับ เพราะพิสูจน์สภาพตอนวัดไม่ได้

**ตัวเลขตัวไหนถูกเผยแพร่อยู่กี่ที่ และวัดใหม่ด้วยคำสั่งอะไร** อยู่ที่
[docs/PUBLISHED-NUMBERS.md](docs/PUBLISHED-NUMBERS.md) — เอกสารนี้เป็นเจ้าของกติกา เอกสารนั้นเป็นเจ้าของที่อยู่

---

## 1. กติกา

ตัวเลขจะเทียบกันได้ก็ต่อเมื่อทุกตัวเจอเงื่อนไขเดียวกัน ข้อที่พังบ่อยที่สุดคือข้อ 3 กับข้อ 9

| # | กติกา | ทำไม |
|---|---|---|
| 1 | เครื่องเดียวกัน รอบเดียวกัน | เอาตัวเลขคนละวันมาปนกันคือคนละเครื่องในทางปฏิบัติ — โหลดเครื่องต่างกัน |
| 2 | ปิดทุกโปรแกรมที่ไม่เกี่ยวก่อนเริ่ม รวมเบราว์เซอร์ | RAM ที่เครื่องเหลืออยู่มีผลต่อ working set โดยตรง |
| 3 | **สภาพเดียวกันทุกตัว** — เปิดเปล่า ไม่เปิดโปรเจกต์ ไม่ล็อกอิน ไม่ลงส่วนเสริม | เทียบแอปเปิดเปล่ากับแอปที่รันงานอยู่ = ตัวเลขปลอม แม้ทั้งคู่จะวัดถูกต้อง |
| 4 | 5 รอบ · ทิ้งรอบแรก · ที่เหลือเอา median | รอบแรกคือ cold รอบถัดไปคือสิ่งที่ผู้ใช้เจอทุกวัน ค่ากลางกันตัวหลุด |
| 5 | รอให้นิ่ง 60 วิ ก่อนอ่าน RAM | Chromium/Electron โหลดต่อหลังหน้าต่างขึ้นแล้ว วัดเร็วไปได้ตัวเลขต่ำเกินจริง |
| 6 | นับทุก process ของแอปนั้น | แอปยุคนี้ไม่มีตัวไหนเป็น process เดียว ดูแต่ตัวแม่คือดูไม่ครบ |
| 7 | ใช้ **private bytes** ไม่ใช่ working set | working set ขึ้นลงตาม RAM ที่เหลือของเครื่อง เทียบข้ามรอบไม่ได้ |
| 8 | จดเวอร์ชันทุกตัว | อีก 3 เดือนตัวเลขชุดนี้ไม่มีความหมายถ้าไม่รู้ว่าวัดเวอร์ชันไหน |
| 9 | **นับเฉพาะสิ่งที่ผู้ใช้ได้ลงไปจริง** — ไม่นับแคช โปรไฟล์ ไฟล์ dev ค้าง | ดูข้อ 5 ข้างล่าง ข้อนี้ทำตัวเลขเพี้ยนได้เป็นเท่าตัว ทั้งฝั่งเราและฝั่งเขา |

**เรื่อง cold start:** "เปิดครั้งแรกจริง ๆ" ต้องรีบูตก่อนแล้ววัดรอบเดียว ถ้าเครื่องเปิดมานานแล้ว ไฟล์ของแอปค้างอยู่ใน file cache ของ Windows รอบ "แรก" จะเร็วกว่าความจริง — `bench.ps1 -Start` เตือนให้เองเมื่อเจอกรณีนี้

---

## 2. เครื่องที่ใช้วัด

| | |
|---|---|
| CPU | Intel Core i5-14450HX · 10 คอร์ / 16 เธรด — **แก้ 2026-09-08** ดูหมายเหตุใต้ตาราง |
| RAM | 32 GB |
| ดิสก์ | GIGABYTE AG4512G-SI B10 (NVMe) · C: 459 GB |
| OS | Windows 11 Home Single Language · build 26200 |

เครื่องเดียว ยังไม่ได้ทวนบนเครื่องสเปกต่ำ — ผลบนเครื่องช้ากว่านี้อาจต่างออกไป โดยเฉพาะเวลาเปิด

> **แก้ 2026-09-08 — CPU ที่จดไว้ไม่ตรงกับเครื่อง** เดิมเขียน `i7-13620H` แต่ `Win32_Processor`
> กับ header ของ `go test -bench` รายงานตรงกันว่าเป็น **`i5-14450HX`** (คอร์/เธรดตรงกันทั้งคู่ที่ 10/16
> ซึ่งน่าจะเป็นเหตุผลที่ไม่มีใครสังเกต)
>
> **บอกไม่ได้ว่าจดผิดมาแต่แรก หรือเปลี่ยนเครื่องระหว่างทาง** — ถ้าเป็นอย่างหลัง ตัวเลขที่วัดก่อน
> 2026-09-08 ทั้งหมดอยู่คนละ CPU กับตัวเลขหลังจากนี้ ซึ่งชนกติกาข้อ 1 เต็ม ๆ
> กระทบหนักสุดคือ**ตัวเลขเวลา** (ข้อ 7 เวลาเปิด · ข้อ 8 ความเร็ว tool) ส่วน**ขนาดไฟล์ (ข้อ 4) ไม่กระทบ**
> เจอเพราะบังเอิญไปอ่าน header ของ `go test -bench` ไม่ใช่เพราะมีอะไรคอยตรวจ — ซึ่งแปลว่ายังไม่มีอะไรคอยตรวจ

---

## 3. คู่แข่งที่เอามาเทียบ

ทุกตัวในตารางนี้**ลงอยู่บนเครื่องเดียวกัน** ไม่มีตัวไหนเอาตัวเลขมาจากเว็บ

| กลุ่ม | ใครบ้าง | เทียบกับ Aetox ได้แค่ไหน |
|---|---|---|
| **ผู้ช่วย AI เดสก์ท็อป** | Comet (Perplexity) · Qwen | ใกล้ที่สุด — เป็นแอปมีหน้าต่าง คุยกับ AI ได้ ไม่ใช่ IDE |
| **IDE ที่มี AI ในตัว** | Cursor · Windsurf · Antigravity (Google) · Zed · VS Code | คนละงานกับ Aetox แต่แย่ง RAM และพื้นที่บนเครื่องเดียวกัน |
| **พิมพ์คำสั่ง (CLI)** | Claude Code · Codex · OpenCode · Copilot CLI | ไม่มีหน้าต่าง เทียบเวลาเปิดไม่ได้ · เทียบได้แค่ขนาด |
| **ตัวรันโมเดล** | Ollama | คนละประเภท เก็บไว้อ้างอิงเฉย ๆ **ห้ามเอาไปเทียบตรง ๆ** |

**Manus** ยังไม่มีอะไรลงเครื่อง — เป็นบริการบนเว็บล้วน ไม่มีตัวเลขให้วัด ไม่ต้องใส่ในตารางเปรียบเทียบ

---

## 4. ผล 1 — พื้นที่ในเครื่อง ✅ วัดครบแล้ว

คู่แข่งวัด 2026-07-27 ครบทุกตัวในรอบเดียว · Aetox วัด 2026-08-25 บน v1.5.7 — คนละวันกัน

| เครื่องมือ | กลุ่ม | ขนาด | เท่าของ Aetox |
|---|---|---|---|
| **Aetox** | ผู้ช่วย AI | **48.5 MB** | — |
| Copilot CLI | CLI | 137 MB | 3× |
| Claude Code | CLI | 236 MB | 5× |
| Zed | IDE | 419 MB | 9× |
| Qwen | ผู้ช่วย AI | 457 MB | 9× |
| OpenCode | CLI | 498 MB | 10× |
| Comet (Perplexity) | ผู้ช่วย AI | 501 MB | 10× |
| Codex (npm) | CLI | 698 MB | 14× |
| Cursor | IDE | 874 MB | 18× |
| Windsurf | IDE | 940 MB | 19× |
| Antigravity (Google) | IDE | 1,030 MB | 21× |
| Codex (ตัวติดตั้งเนทีฟ) | CLI | 1,123 MB | 23× |
| VS Code | IDE | 1,171 MB | 24× |
| Ollama _(คนละประเภท)_ | รันโมเดล | 2,812 MB | 58× |

ตัวติดตั้งที่ผู้ใช้โหลด **21.3 MB** · ตัวโปรแกรมที่ลงแล้ว **48.5 MB** (`aetox.exe` ไฟล์เดียวจบ)

**Codex ลงได้สองทาง ขนาดต่างกันเกือบเท่าตัว** — ผ่าน npm 698 MB, ตัวติดตั้งเนทีฟ 1,123 MB
เวลาอ้างอิงต้องบอกด้วยว่าอันไหน เพราะบนเครื่องนี้ `codex` ที่อยู่ใน PATH ชี้ไปที่ตัว npm

**เหตุผลที่เล็กได้ขนาดนี้:** Go คอมไพล์เป็นไฟล์เดียว ไม่ต้องแบก Node.js runtime มาด้วย และหน้าจอใช้ WebView2 ที่ Windows มีอยู่แล้ว ไม่ได้บรรจุ Chromium สำเนาของตัวเองมาเหมือน Electron

---

## 5. บทเรียนจากรอบวัดแรก — ของที่ห้ามนับ

รอบแรกได้ตัวเลขผิดทั้งฝั่งเราและฝั่งคู่แข่ง เพราะไปนับทั้งโฟลเดอร์แบบไม่ดูข้างใน จดไว้กันพลาดซ้ำ

| เครื่องมือ | นับทั้งโฟลเดอร์ได้ | ของจริง | ที่ปนเข้ามา |
|---|---|---|---|
| **Aetox** | 93 MB | **33 MB** | `aetox-dev.exe~` 47.5 MB (ไฟล์ dev ค้าง) + ตัวติดตั้ง 12.6 MB |
| **Comet** | 2,001 MB | **501 MB** | `Comet\User Data` 1,283 MB — โปรไฟล์กับแคชเบราว์เซอร์ |
| **Claude Code** | 301 MB | **236 MB** | `uv.exe` 64.6 MB กับ python shim ของเจ้าของเครื่องที่บังเอิญอยู่ใน `.local\bin` |

ถ้าปล่อยไว้ Aetox จะดูใหญ่กว่าความจริง 3 เท่า และ Comet จะดูใหญ่กว่าความจริง 4 เท่า — **ผิดทั้งสองทาง**
ตัวที่เข้าข้างเราคือตัวที่อันตรายที่สุด เพราะไม่มีใครอยากตรวจตัวเลขที่ทำให้ตัวเองดูดี

**กติกาที่ได้จากบทเรียนนี้ (ข้อ 9):** `Dir` ในสคริปต์ต้องชี้ไปที่สิ่งที่ผู้ใช้ได้ลงจริงเท่านั้น — ชี้ไฟล์เดียวได้ถ้าแอปเป็นไฟล์เดียว และห้ามชี้โฟลเดอร์ที่มีข้อมูลผู้ใช้ปน

### เรื่องเดียวกันเกิดกับ RAM ด้วย

รอบแรกของ `-Snapshot` รายงานว่า Aetox กิน **904 MB / 12 process** ทั้งที่ตอนนั้น Aetox ไม่ได้เปิดอยู่เลยสักตัว

สาเหตุ: Aetox ใช้ WebView2 ซึ่งทุกแอปบนเครื่องที่ใช้ WebView2 ต่างก็รัน `msedgewebview2.exe` ชื่อเดียวกันหมด นับตามชื่อ process เฉย ๆ เลยกวาด RAM ของแอปอื่นมาเป็นของเรา

แก้ด้วยการกรอง command line (`CmdMatch` ใน `$Apps`) — `-Snapshot` กับ `-Soak` ไม่มี baseline ก่อน/หลัง จึงต้องพึ่งการกรองนี้ทั้งหมด

**แก้เพิ่ม 2026-07-28 — ประโยคเดิมตรงนี้เขียนว่า "โหมด `-Start` ไม่เจอปัญหานี้" ซึ่งผิด** โหมดนั้นมีสองจุดที่นับ process ไม่ใช่จุดเดียว: การวัดใช้ baseline ก่อน/หลังจริงตามที่เขียนไว้ แต่**ด่านที่ถามว่า "แอปเปิดค้างอยู่หรือเปล่า" ไม่มี baseline ให้เทียบ** และตอนนั้นยังไม่ได้กรอง `CmdMatch` ผลคือมันนับ `msedgewebview2.exe` ของแอปอื่นเป็นของ Aetox แล้วข้ามการวัดไปเลย — บนเครื่องที่มีแอป WebView2 ตัวไหนเปิดอยู่ ซึ่งคือเครื่องส่วนใหญ่ ตอนวัดรอบ 2026-07-28 มันขึ้นว่า "เปิดค้างอยู่ 12 process" ขณะที่ `-Snapshot` ไม่เห็น Aetox สักตัว

> บทเรียนซ้อนบทเรียน: รู้ว่าปัญหาคืออะไรแล้ว แก้ที่จุดหนึ่ง แล้วเขียนลงเอกสารว่า "โหมดนั้นไม่เจอ" โดยไม่ได้ไล่ดูว่าโหมดนั้นนับ process กี่ที่

> จุดร่วมของบทเรียนทั้งสองข้อ: **นับของที่ไม่ใช่ของตัวเอง** ไม่ว่าจะเข้าข้างเราหรือไม่ ก็คือตัวเลขผิดเหมือนกัน

### บทเรียนที่สาม — working set นับหน้าที่แชร์กันซ้ำ (วัด 2026-09-08)

กติกาข้อ 7 ให้ใช้ private bytes โดยให้เหตุผลว่า working set ขึ้นลงตาม RAM ที่เครื่องเหลือ — จริง
แต่มีเหตุผลที่สองซึ่งแรงกว่าและเพิ่งวัดเจอ: **WS นับหน้าหน่วยความจำที่แชร์กันซ้ำทุกโปรเซส**

Aetox v1.5.19 ตอนเปิดใช้งานอยู่ 7 process — **WS รวม 687 MB แต่ private รวม 449 MB ห่างกัน 238 MB**

238 MB นั้นคือ DLL ของ Chromium ชุดเดียวที่ถูก map เข้าไปในทั้ง 6 โปรเซส WebView2 แล้วถูกนับซ้ำหกรอบ
เห็นชัดที่สุดที่ browser process: WS 131 MB แต่ private แค่ 40 MB — 91 MB ที่เกินมาไม่ใช่ของมัน

> ผลในทางปฏิบัติ: ใครเปิด Task Manager ดูคอลัมน์ Memory จะเห็น Aetox ~690 MB ทั้งที่กินจริง 449 MB
> — **บวมเกินจริง 53%** และยิ่งแอปแตกโปรเซสเยอะยิ่งบวมมาก เวลาเอา WS ไปเทียบกับแอปที่มีโปรเซสน้อยกว่า
> เราจะเสียเปรียบฟรี ๆ ทั้งที่กินน้อยกว่า

---

## 6. ผล 2 — RAM ตอนทำงาน ⚠️ วัดได้ไม่ครบ

**ตัวเลขที่สำคัญที่สุดในเอกสารนี้** ผู้ใช้เจอมันตลอดเวลาที่เปิดโปรแกรมไว้ ต่างจากขนาดติดตั้งที่เจอครั้งเดียวตอนลง

### ที่ผ่านกติกาแล้ว

| เครื่องมือ | private | process | สภาพตอนวัด | ขึ้นเว็บได้ |
|---|---|---|---|---|
| **Aetox** | **252 MB** | 7 | เปิดเปล่า นิ่งแล้ว | ✅ |
| Zed | 471 MB | 4 | เปิดเปล่า นิ่งแล้ว | ✅ |

Aetox กิน RAM ราวครึ่งเดียวของ Zed ทั้งที่ Zed เป็นแอป Rust เนทีฟที่ขึ้นชื่อเรื่องเบา

### ที่ยังไม่ผ่านกติกา — ห้ามขึ้นเว็บจนกว่าจะวัดใหม่

| เครื่องมือ | private | process | สภาพตอนวัด | ปัญหา |
|---|---|---|---|---|
| Cursor | 1,750 MB | 10 | เปิดใช้งานจริง ~6 นาที พร้อมส่วนเสริมของเจ้าของเครื่อง | ผิดกติกาข้อ 3 |
| VS Code | 4,814 MB | 24 | กำลังรัน Claude Code session อยู่ | ผิดกติกาข้อ 3 หนักสุด |
| Claude Code | 4,497 MB | 18 | กำลังทำงานจริงระหว่าง session เดียวกันนั้น | ผิดกติกาข้อ 3 · เป็น CLI ไม่มีสภาพ "เปิดเปล่า" |

> ตัวเลข 4,814 MB ของ VS Code คือ VS Code ที่กำลังแบก AI agent session ของเราเองอยู่ ไม่ใช่ VS Code เปิดเปล่า
> ถ้าเอาไปเทียบกับ Aetox เปิดเปล่าจะได้ "เบากว่า 19 เท่า" ซึ่งอ่านแล้วดูดีแต่ไม่จริง

### ยังต้องวัด

- [ ] Cursor · Windsurf · Antigravity · VS Code · Comet · Qwen — เปิดเปล่าทั้งหมด รอบเดียวกัน
- [ ] **Aetox ตอนทำงานจริง** — คุยไปสัก 20 เทิร์น เปิดแท็บเบราว์เซอร์ ใช้เครื่องมือครบ แล้ววัดใหม่
      ข้อนี้สำคัญที่สุด: ตอนนี้เรามีแค่ตัวเลขเปิดเปล่าของตัวเอง ถ้าจะเทียบกับตัวอื่น "ตอนใช้งานจริง" ก็ต้องมีของเราด้วย
      **มีภาพถ่ายสภาพทำงานจริงแล้วเมื่อ 2026-09-08 (ดูข้างล่าง) แต่ยังไม่ปิดรายการนี้** —
      รอบนั้นไม่ได้จดว่าเซสชันมีกี่เทิร์น จึงทำซ้ำให้เหมือนเดิมไม่ได้ ซึ่งคือหัวใจของข้อ 3

### Aetox ตอนทำงานจริง — ภาพถ่าย 2026-09-08 ⚠️ ไม่ผ่านกติกา ห้ามขึ้นเว็บ

**ผิดกติกาข้อ 2** (ไม่ได้ปิดโปรแกรมอื่น) · **ข้อ 3** (ไม่ใช่สภาพเปิดเปล่า) · **ข้อ 4** (รอบเดียว ไม่ใช่ median 5 รอบ) · **ข้อ 5** (ไม่ได้รอนิ่ง 60 วิ)

เก็บไว้เพราะมันตอบคำถามที่ตัวเลข "252 MB เปิดเปล่า" ตอบไม่ได้เลย: **RAM ไปกองอยู่ตรงไหนข้างใน** — ไม่ใช่เพื่อเอาไปเทียบกับใคร

Aetox v1.5.19 (ตัวที่ลงใน Program Files ไม่ใช่ dev build) เปิดค้างระหว่างเซสชันทำงานจริง:

| process | private | WS | ใครคุมตัวเลขนี้ |
|---|---|---|---|
| gpu-process | **196 MB** | 210 MB | CSS ของเรา — composited layer × ขนาดหน้าต่าง × DPI 1.75 |
| renderer | **136 MB** | 194 MB | หน้าบ้านของเรา — DOM + JS heap |
| aetox.exe (Go) | 53 MB | 79 MB | หลังบ้านของเรา |
| browser | 40 MB | 131 MB | Chromium ตายตัว |
| NetworkService | 12 MB | 38 MB | Chromium ตายตัว |
| StorageService | 9 MB | 22 MB | Chromium ตายตัว |
| crashpad-handler | 3 MB | 13 MB | Chromium ตายตัว |
| **รวม** | **449 MB** | **687 MB** | |

สามอย่างที่ตัวเลขชุดนี้บอก และตัวเลขเปิดเปล่าไม่เคยบอก:

1. **ฝั่ง Go ไม่ใช่ปัญหา** — 53 MB จาก 449 MB คือ 12% ต่อให้บีบจนเหลือศูนย์ก็ยังเหลือ 396 MB
   ข้อนี้ปิดประตูเรื่อง "ใช้ความสามารถของ Go compiler ลดแรม" ไปเลย: inlining กับ escape analysis
   เปิดอยู่แล้วโดยปริยาย ส่วน PGO ให้ CPU ไม่ใช่ RAM และทำให้ไบนารีโตขึ้นด้วยซ้ำ
2. **gpu-process แซงขึ้นเป็นที่หนึ่ง** ที่ 196 MB — และนั่นคือ VRAM ซึ่งบนเครื่องนี้แย่งกับ LM Studio/Ollama
3. **ส่วนของ Chromium ที่เราแตะไม่ได้มีแค่ 64 MB** (browser + network + storage + crashpad) น้อยกว่าที่เดาไว้มาก
   ที่เหลือ 385 MB คือ gpu + renderer ซึ่ง**เป็นของเราทั้งคู่** แปลว่าลดได้ ถ้าจะลง

**ยังไม่รู้และไม่ได้จดไว้ตอนวัด:** เซสชันที่เปิดค้างมีกี่เทิร์น เปิดแท็บเบราว์เซอร์ไว้กี่แท็บ เปิดมานานเท่าไร
เพราะงั้นเอาไปเทียบกับรอบวัดหน้าไม่ได้ — **นี่คือเหตุผลที่มันยังไม่ปิดรายการ "Aetox ตอนทำงานจริง" ข้างบน**

### ทั้งเครื่องเป็นยังไงตอนนั้น ⚠️ ภาพถ่าย ไม่ใช่การเทียบ

**ทุกแอปในตารางนี้อยู่คนละสภาพกัน — ห้ามอ่านเป็นตารางเปรียบเทียบ** ผิดกติกาข้อ 3 ทุกแถวโดยไม่มีข้อยกเว้น
มีไว้ตอบคำถามเดียว: บนเครื่องที่ทำงานจริง Aetox ยืนอยู่ตรงไหนของแถว

เครื่องตามข้อ 2 · RAM 31.7 GB ว่าง 13.2 GB · 2026-09-08 01:45 · นับแบบยุบโปรเซสลูกเข้าแอปแม่ทุกตัว

| แอป | private | process | เวอร์ชัน | สภาพตอนวัด |
|---|---|---|---|---|
| claude | 8,400 MB | 65 | 1.46388.4 | **กำลังรัน agent session ที่วัดเรื่องนี้อยู่** |
| Antigravity IDE | 2,608 MB | 45 | 2.5.5 | เปิดใช้งานอยู่ |
| chrome | 1,485 MB | 15 | 152.0.7977.82 | เปิดใช้งานอยู่ |
| ChatGPT | 1,285 MB | 12 | 152.0.7977.83 | เปิดใช้งานอยู่ |
| Discord | 1,194 MB | 6 | 1.0.9256 | เปิดค้าง ไม่ได้ใช้ |
| steam | 1,037 MB | 7 | 10.96.30.42 | เปิดค้าง ไม่ได้ใช้ |
| dwm | 1,022 MB | 1 | — | ตัวประกอบหน้าต่างของ Windows เอง ไม่ใช่แอป |
| LINE | 610 MB | 10 | 26.4.2.3957 | เปิดค้าง ไม่ได้ใช้ |
| NVIDIA Broadcast | 588 MB | 6 | 2.2.0.10184 | เปิดค้าง |
| **Aetox** | **455 MB** | **7** | **1.5.19** | **เปิดใช้งานอยู่** |
| SearchHost | 341 MB | 7 | 2606.30004.100.0 | Windows Search ไม่ใช่แอปที่เราเปิด |
| msedge | 272 MB | 7 | 152.0.4191.66 | เปิดค้าง |
| Widgets | 258 MB | 7 | 526.21100.10.0 | วิดเจ็ตของ Windows |

> **วิธีอ่านที่ถูกต้อง:** ตารางนี้ไม่ได้แปลว่า Aetox "ประหยัดกว่า Discord 2.6 เท่า" — Discord เปิดค้างเฉย ๆ
> ส่วน Aetox กำลังทำงานอยู่ คนละสภาพ เทียบไม่ได้ตามกติกาข้อ 3
> สิ่งเดียวที่ตารางนี้พิสูจน์ได้จริงคือ **Aetox ไม่ใช่ตัวที่กิน RAM มากบนเครื่องนี้** แค่นั้น
>
> และแถวบนสุดคือเครื่องเตือนใจว่ากติกาข้อ 3 มีไว้ทำไม: **ตัวที่กินหนักที่สุด 8.4 GB คือ agent ที่กำลังวัดอยู่เอง**

> **ตัวเลขขยับเองระหว่างวัด:** สคริปต์เดียวกันอ่าน Aetox ได้ 449 MB ในส่วนหนึ่ง และ 455 MB อีกส่วนหนึ่ง
> ที่ห่างกันไม่ถึงวินาที — **±6 MB คือพื้น noise ของการวัดแบบภาพถ่าย** ความต่างที่น้อยกว่านี้ระหว่างสองรอบวัด
> แปลว่าไม่ต่าง ไม่ใช่ดีขึ้นหรือแย่ลง

#### กับดัก WebView2 ของข้อ 5 — ตรวจซ้ำแล้วยังอยู่

ตอนวัดมี `msedgewebview2.exe` ทั้งเครื่อง **24 process** แต่เป็นของ Aetox แค่ 5:

| เจ้าของ (`--webview-exe-name`) | process | private |
|---|---|---|
| **aetox.exe** | 5 | **393 MB** |
| SearchHost.exe | 5 | 252 MB |
| Widgets.exe | 5 | 242 MB |
| LINE.exe | 5 | 136 MB |
| ไม่ระบุ (crashpad ของแต่ละตัว) | 4 | 11 MB |

ถ้ากวาดตามชื่อ process เฉย ๆ Aetox จะโดนยัด **630 MB ของแอปอื่น** มาเป็นของตัวเอง — ตรงกับที่ข้อ 5 บันทึกไว้
`CmdMatch` ใน [bench.ps1](bench.ps1) ยังทำงานถูกต้อง ตรวจซ้ำแล้ว

---

## 7. ผล 3 — ความเร็วตอนเปิด ⚠️ วัดได้ 2 ตัว

| เครื่องมือ | เปิดครั้งแรก (cold) | เปิดครั้งถัดไป | สภาพตอนวัด | ขึ้นเว็บได้ |
|---|---|---|---|---|
| **Aetox** | **1.77 วิ** | **0.53 วิ** | เปิดเปล่า · median 5 รอบ | ✅ |
| Zed | 2.12 วิ | 0.53 วิ | เปิดเปล่า · median 5 รอบ | ✅ |
| Cursor | — | — | เปิดจากสคริปต์ไม่ได้ | ⬜ |
| Windsurf · Antigravity · VS Code · Comet · Qwen | — | — | ยังไม่ได้วัด | ⬜ |

จับเวลาถึงตอน **หน้าต่างโผล่บนจอ** (`MainWindowHandle != 0`) ไม่ใช่ตอน process เกิด — เป็นเอเจนของ "เห็นแล้วกดได้" ที่ใกล้ความจริงที่สุดเท่าที่วัดอัตโนมัติได้

**ทำไม Cursor ถึงจับเวลาไม่ได้ในรอบแรก:** `Cursor.exe` ที่เรียกทำหน้าที่เป็น launcher มันโยนงานให้ process อื่นแล้วจบตัวเองทันที สคริปต์เลยไม่มี handle ให้ตาม — Windsurf กับ Antigravity เป็น VS Code fork เหมือนกัน เจอปัญหาเดียวกัน

**แก้ในสคริปต์แล้ว (ยังไม่ได้ทดสอบกับของจริง):** `Wait-Window` มองหาหน้าต่างของ process ที่เกิดใหม่ทุกตัว ไม่ใช่แค่ตัวที่สั่งเปิด และ `Stop-App` ปิดแบบกดกากบาทก่อนค่อยบังคับปิด เพราะการฆ่าดิบ ๆ ทุกรอบจะทำให้รอบถัดไปเจอหน้าต่าง "กู้คืนเซสชันที่ปิดผิดปกติ" แล้วเวลาเปิดเพี้ยนทั้งชุด — ถ้ารอบวัดจริงยังจับไม่ได้ ให้จดไว้ตรงนี้ว่าเป็นข้อจำกัดถาวร

**ทำไม cold ของ Aetox ช้ากว่า warm 3 เท่า:** รอบแรก Windows ต้องอ่าน binary กับ WebView2 runtime จากดิสก์จริง รอบถัดไปอยู่ใน cache แล้ว — 0.53 วิ คือสิ่งที่ผู้ใช้เจอแทบทุกครั้ง ส่วน 1.77 วิ เจอครั้งเดียวหลังรีบูต

**วัดซ้ำ 2026-07-28 บน build ใหม่ ⚠️ ห้ามขึ้นเว็บ** (หลังเพิ่ม shell/git/background/vision/undo/notebook/symbol): เปิดครั้งแรก 1.96 วิ · median รอบถัดไป **0.53 วิ** · RAM 276 MB private · 7 process

รอบนี้รัน **3 รอบ รอให้นิ่ง 25 วิ** ซึ่ง**ผิดกติกาข้อ 4 (5 รอบ) และข้อ 5 (60 วิ)** จึงเอาไปแทนตัวเลขบนหน้าเว็บไม่ได้ — เก็บไว้ในฐานะ**การตรวจว่าไม่ถอยหลัง** เท่านั้น และมันตอบคำถามที่ตั้งใจถาม: tools เพิ่มจาก 20 เป็น 27 ตัว payload เกือบเท่าตัว **เวลาเปิดเท่าเดิม 0.53 วิ** · RAM ขยับจาก 252 เป็น 276 MB ซึ่งอยู่ในช่วงที่ตัวเลขจากคนละกติกาบอกอะไรไม่ได้ ต้องวัดใหม่ให้ครบกติกาก่อนจึงจะรู้ว่าโตจริงหรือแค่ noise

**บั๊กในสคริปต์ที่เจอตอนวัดรอบนี้ (แก้แล้ว):** `-Start` เช็คว่าแอปเปิดค้างอยู่ไหมด้วย `Get-PidSet` ซึ่ง **ไม่กรอง `CmdMatch`** ต่างจาก `Measure-Procs` — `msedgewebview2.exe` เป็นชื่อกลางที่ทุกแอป WebView2 บนเครื่องใช้ร่วมกัน (Windows เองก็ใช้) มันเลยนับ process ของแอปอื่นเป็นของ Aetox แล้วขึ้นว่า "เปิดค้างอยู่ 12 process" ทั้งที่ `-Snapshot` ไม่เห็นสักตัว ผลคือ **`-Start` ข้าม Aetox ตลอดกาลบนเครื่องที่มีแอป WebView2 ตัวไหนเปิดอยู่ ซึ่งคือเครื่องส่วนใหญ่** — สคริปต์เตือนเรื่องชื่อกลางนี้ไว้ในคอมเมนต์ของตัวเองแล้ว แต่ด่านนี้ไม่ได้ใช้

---

## 8. ผล 4 — ข้างในเครื่องยนต์ ✅ วัด 2026-07-28

ข้อ 4–7 วัดสิ่งที่ผู้ใช้รู้สึก **ตอนเปิดแอป** ครั้งเดียว ข้อนี้วัดสิ่งที่เขารู้สึก **ทุกครั้งที่พิมพ์** ซึ่งเจอบ่อยกว่ากันหลายพันเท่าและไม่เคยวัดมาก่อน

รันด้วย `.\bench.ps1 -Engine` · เครื่องเดียวกับข้อ 2

### สิ่งที่จ่ายทุกคำขอ

| | ค่า |
|---|---|
| tools ที่โมเดลเห็น | **27 ตัว** (registry 30 รายการ — `echo`/`fs`/`help` ไม่ถูกส่ง) |
| payload ต่อคำขอ | **18.1 KB** |
| เวลาประกอบ + แปลง JSON | **0.12 ms/เทิร์น** · จอง 96.2 KB |

payload ไปกับ **ทุก** คำขอ ก่อนบทสนทนาเสียอีก — เป็นทั้งค่าโทเคนที่ผู้ใช้จ่ายและงานที่เครื่องทำ · เดิม 20 tools = 9.9 KB วันนี้ 27 tools = 18.1 KB **แต่เวลาประกอบไม่ขึ้น** (0.13 → 0.12 ms)

### ความเร็ว tool

| | เวลา | หมายเหตุ |
|---|---|---|
| grep ทั้ง repo จริง | **45.5 ms** | เดินทั้งโปรเจกต์ 12,000 ไฟล์ ข้าม node_modules |
| ตรวจ path ในแซนด์บ็อกซ์ | **728 µs** | **ทุก tool ที่แตะไฟล์จ่ายอันนี้ร่วมกัน** |
| ตั้งชื่อ tool call ตอนสตรีม | **2.6 µs** | ต่อ chunk ที่ไหลเข้ามา |
| สะสมสตรีมไฟล์ใหญ่ | **1.85 ms** | ตอนโมเดลเขียนไฟล์ยาว ๆ |

728 µs ของการตรวจ path ตรงกับที่ §29 ของ ARCHITECTURE.md บันทึกไว้ (~0.6–1 ms คือราคาของ symlink containment ใน §28) — วัดใหม่แล้วยังอยู่ในช่วงเดิม และยังเป็นฝุ่นเทียบกับ round trip ของโมเดล การแคช root จึงยังเป็น `ponytail:` note ไม่ใช่โค้ด

### ตอนสร้างและตอนเทส

| | เวลา |
|---|---|
| `wails build` (ตัวแอปจริง) | **43.3 วิ** |
| `go test ./...` | **38.5 วิ** · 26 แพ็กเกจ · ผ่านหมด |
| `aetox.exe` ที่ได้ | **33.1 MB** |

38.5 วิ ไม่ใช่แค่ตัวเลขความเร็ว — เป็นเพดานว่า agent สั่ง `go test ./...` เองแล้วจะทันไหม เพดาน tool คือ 60 วิ ต่อคำสั่ง ปรับได้ถึง 600 ด้วย `timeout_seconds` **ตอนนี้ยังเหลือที่ 21.5 วิ ก่อนชนเพดานเริ่มต้น**

**ไม่มีคู่แข่งในตารางนี้ตั้งใจ** — ตัวเลขข้างในของ Claude Code/OpenCode วัดจากข้างนอกไม่ได้ เอามาเทียบก็คือเดา ที่นี่จึงวัดเราเทียบกับเราเองข้ามเวอร์ชัน

---

## 9. ผล 5 — ความเสถียร

### ตอน build ✅

| | ผล |
|---|---|
| `go test ./...` | **745 / 745** · 24 package · 0 fail |
| เทสต์ฝั่งหน้าจอ | **83 / 83** |
| ตรวจชนิดข้อมูล | Go + TypeScript + `svelte-check` ทั้งสองฝั่ง |

### ตอนใช้งานยาว ๆ ⬜ ยังไม่ได้วัด

ช่องว่างใหญ่ที่สุดของเอกสารนี้ตอนนี้ เทสต์ 745 ตัวบอกได้แค่ว่าโค้ดถูกตอนคอมไพล์ ไม่ได้บอกว่าเปิดทิ้งไว้ 8 ชั่วโมงแล้วยังดีอยู่ไหม

รันด้วย `.\bench.ps1 -Soak -Hours 8` แล้วดู 3 อย่าง:

| ดูอะไร | เกณฑ์ตก |
|---|---|
| RAM โตขึ้นเรื่อย ๆ ไม่ลงเลย | โตเกิน ~10% ต่อชั่วโมงแบบไม่กลับลง = น่าจะรั่ว |
| process หายไประหว่างทาง | แอปดับเอง = ตกทันที |
| หน่วงลงเมื่อแชทยาวขึ้น | จดเวลาตอบของเทิร์นที่ 1 เทียบเทิร์นที่ 50 |

- [ ] Aetox soak 8 ชม.
- [ ] Cursor soak 8 ชม. (สภาพเดียวกัน)

---

## 10. คู่แข่งที่ยังไม่ได้ลง

**ไม่เอาตัวเลขจากเว็บมาใส่ตาราง** — ขนาดตัวติดตั้งที่เขาประกาศ ไม่เท่ากับขนาดหลังลงจริง (Aetox เองต่างกัน 21.3 vs 48.5 MB) เอามาปนกับตัวเลขที่วัดเองจะพังทั้งตาราง ตัวไหนอยากได้ก็ลงแล้ววัด ใช้เวลาเพิ่มตัวละไม่กี่นาที

| เครื่องมือ | กลุ่ม | ลงยังไง |
|---|---|---|
| Trae (ByteDance) | IDE | โหลดจากเว็บ |
| Kiro (AWS) | IDE | โหลดจากเว็บ |
| Void | IDE | โหลดจากเว็บ |
| JetBrains + AI Assistant | IDE | Toolbox |
| Warp | เทอร์มินัล AI | โหลดจากเว็บ |
| LM Studio | ผู้ช่วย AI | เครื่องนี้เหลือแต่โฟลเดอร์ updater — ลงใหม่ก่อนถึงจะวัดได้ |
| Jan · Msty · Cherry Studio · AnythingLLM | ผู้ช่วย AI | โหลดจากเว็บ — กลุ่มนี้ใกล้ Aetox ที่สุด ควรลงก่อนเพื่อน |
| Gemini CLI | CLI | `npm i -g @google/gemini-cli` |
| Aider | CLI | `uv tool install aider-chat` |
| Goose (Block) | CLI | โหลดจากเว็บ |
| Amp (Sourcegraph) | CLI | `npm i -g @sourcegraph/amp` |
| Crush (Charm) | CLI | `npm i -g @charmland/crush` |

เพิ่มเข้าไปวัดได้ด้วยการเติมแถวเดียวใน `$Apps` ที่หัวไฟล์ [bench.ps1](bench.ps1) — ตัวไหนไม่มีบนเครื่อง สคริปต์ข้ามให้เอง

---

## 11. ที่วัดไม่ได้ และเพราะอะไร

บอกไว้ให้ครบ เพราะการยอมรับว่าอะไรวัดไม่ได้คือสิ่งที่ทำให้ตัวเลขที่เหลือน่าเชื่อ

| เครื่องมือ | วัดไม่ได้เพราะ |
|---|---|
| Claude Code · Codex · OpenCode · Copilot CLI | ไม่มีหน้าต่างแอป จับเวลาเปิดไม่ได้ และไม่มีสภาพ "เปิดเปล่า" ให้เทียบ RAM — เทียบได้แค่ขนาด |
| Manus | บริการบนเว็บล้วน ไม่มีอะไรลงเครื่อง — **ไม่ต้องเอาเข้าตารางเลย** ใส่ไปก็เป็นแถวว่าง |
| VS Code (เวลาเปิด/RAM) | ยังไม่ได้วัด ต้องปิด session ที่กำลังทำงานอยู่ก่อน |
| Cursor (เวลาเปิด) | launcher จบตัวเองทันที สคริปต์ตามไม่ทัน — ดูข้อ 7 |

> **ต้องพูดตรง ๆ เรื่อง RAM:** WebView2 ก็คือ Chromium เหมือนกัน ที่เราใช้ RAM น้อยกว่าไม่ใช่เพราะเอนจินวาดหน้าจอวิเศษกว่า
> แต่เพราะมันเป็นส่วนกลางของ OS ที่แชร์กับแอปอื่น ไม่ใช่สำเนาต่อแอป และเพราะเราไม่ได้ยัดทุกอย่างไว้ในโปรแกรมเดียว
> ข้อนี้ต้องอยู่รอดทุกครั้งที่แก้หน้าเว็บ

---

## 12. วิธีรันซ้ำ

```powershell
.\bench.ps1 -Disk                   # ขนาดในเครื่อง — รันได้ทุกเมื่อ ไม่ต้องปิดอะไร
.\bench.ps1 -Snapshot               # อ่าน RAM ของตัวที่เปิดค้างอยู่ ไม่แตะอะไร
.\bench.ps1 -Start                  # เวลาเปิด + RAM — ต้องปิดตัวที่จะวัดก่อน
.\bench.ps1 -Start -App Aetox       # เฉพาะตัวเดียว
.\bench.ps1 -Soak -Hours 8          # เปิดทิ้งไว้แล้วเก็บ RAM ทุก 5 นาที → logs/soak-*.csv
```

**ลำดับที่ถูกต้องสำหรับรอบวัดใหญ่:**

1. รีบูต — ให้ทุกตัวเจอ file cache สภาพเดียวกัน
2. ไม่เปิดอะไรเลย → `.\bench.ps1 -Start` (ได้ cold ของทุกตัวในรอบเดียว)
3. `.\bench.ps1 -Disk`
4. เปิดตัวที่จะ soak แล้ว `.\bench.ps1 -Soak -Hours 8` ทิ้งไว้ข้ามคืน
5. เอาผลมาลงเอกสารนี้ พร้อม**สภาพตอนวัด**ทุกแถว แล้วค่อยยกขึ้นหน้าเว็บ

---

## 13. ประวัติการวัด

| วันที่ | วัดอะไร | ผล |
|---|---|---|
| 2026-09-08 | RAM ตอนทำงานจริง — แยกรายโปรเซส + ภาพถ่ายทั้งเครื่อง | ⚠️ ไม่ผ่านกติกาข้อ 2·3·4·5 · เก็บไว้ดู**โครงสร้าง**เท่านั้น ห้ามขึ้นเว็บ — ดูข้อ 6 |
| 2026-09-08 | ทดลอง PGO (Go 1.27) กับ benchmark สามแพ็กเกจ | ❌ **ไม่เอา** — แอปใช้ CPU แค่ 0.46% · 34% ของโปรไฟล์เป็น `cgocall` ที่ PGO แตะไม่ได้ · before/after จมใน noise ±107% · ไบนารีโต +0.41% |
| 2026-08-25 | ขนาด artifact ของ v1.5.7 จากหน้า release | ✅ ตัวติดตั้ง **21.3 MB** · zip **19.9 MB** — ดูหมายเหตุใต้ตาราง |
| 2026-08-25 | ตารางข้อ 4 หารด้วยตัวเลขที่ตายแล้ว | ❌ ตัวคูณทั้งคอลัมน์เข้าข้างเรา ~30% · แก้แล้ว ดูหมายเหตุใต้ตาราง |
| 2026-08-22 | ขนาดในเครื่องของ v1.5.0 | ✅ **47.9 MB** — ขึ้นหน้าเว็บแล้ว แต่ไม่เคยถูกบันทึกที่นี่จนถึง 25 ส.ค. |
| 2026-08-07 | ขนาดจริงของ v0.9.2 จาก artifact ที่ปล่อยแล้ว | ✅ **40.8 MB** — แทน 33 MB · ดูหมายเหตุใต้ตาราง |
| 2026-07-28 | ข้างในเครื่องยนต์ (payload · ความเร็ว tool · build · เทส) | ✅ ผ่านกติกา — ตารางข้อ 8 · โหมด `-Engine` ใหม่ |
| 2026-07-28 | ขนาดในเครื่อง + เวลาเปิด บน build ใหม่ | ✅ ขนาดเท่าเดิม 33 MB · เปิด 0.53 วิ เท่าเดิมทั้งที่ tools เพิ่ม 20→27 |
| 2026-07-27 | ขนาดในเครื่อง 14 ตัว (รอบแก้แล้ว) | ✅ ผ่านกติกา — ตารางข้อ 4 |
| 2026-07-27 | ขนาดในเครื่อง รอบแรก | ❌ นับของปนเข้ามา ดูข้อ 5 — ยกเลิก |
| 2026-07-27 | Aetox v0.5.0 + Zed เปิดเปล่า (เวลาเปิด + RAM) | ✅ ผ่านกติกา — ขึ้นหน้าเว็บแล้ว |
| 2026-07-27 | Cursor · VS Code · Claude Code ตอนใช้งานจริง | ⚠️ ไม่ผ่านกติกาข้อ 3 · เก็บไว้อ้างอิงเท่านั้น |

### 2026-09-08 — แยกดูว่า RAM ของ Aetox ไปกองอยู่ตรงไหน

คำถามตั้งต้นไม่ใช่ "กินเท่าไร" แต่เป็น "**ถ้าจะลด ต้องไปลดตรงไหน**" ซึ่งตัวเลขรวมตอบไม่ได้
เลยวัดแยกรายโปรเซสเป็นครั้งแรก — ผลอยู่ในข้อ 6

**สิ่งที่ได้มาสามอย่าง**

1. **ตัวที่กินหนักสุดคือ gpu-process (196 MB) ไม่ใช่ renderer** — ผิดจากที่เดากันไว้
   และมันโตตามขนาดหน้าต่าง × DPI × จำนวน composited layer ซึ่งเป็น**ผลจาก CSS ของเราเอง**
2. **Chromium ส่วนที่แตะไม่ได้จริง ๆ มีแค่ 64 MB** จาก 449 MB — ที่เหลือ 385 MB เป็นของเราทั้งหมด
   เดิมเข้าใจกันว่า "WebView2 กินเอง ทำอะไรไม่ได้" ซึ่งไม่จริง
3. **WS บวมเกิน private 238 MB** เพราะนับ DLL ที่แชร์กันซ้ำหกรอบ — บทเรียนข้อสามในข้อ 5

**แก้ความเข้าใจผิดที่เกือบทำให้เสียเวลาเปล่า**

รอบนี้เริ่มจากคำถามว่าจะใช้ความสามารถของ Go compiler (inlining · escape analysis · PGO) ลด RAM ได้ไหม
คำตอบคือ**ไม่ได้ และไม่ใช่เพราะมันไม่เก่ง** — สองตัวแรกเปิดอยู่แล้วโดยปริยายทุกครั้งที่ `go build`
ส่วน PGO ให้ CPU ไม่ใช่ RAM แถมทำให้ไบนารีโตขึ้นเพราะ inline หนักขึ้น
และเหนือกว่านั้น: ทั้งสามตัวทำงานกับ `aetox.exe` ซึ่งกิน **53 MB จาก 449 MB (12%)**
ต่อให้บีบจนเหลือศูนย์ก็ยังเหลือ 396 MB — **เพดานของทางนี้อยู่ต่ำกว่าปัญหา**

> จดไว้เพราะมันเป็นรูปแบบเดิมของข้อ 5 ในอีกหน้าตาหนึ่ง: คราวนั้นนับของที่ไม่ใช่ของตัวเอง
> คราวนี้เกือบไปออปติไมส์ส่วนที่ไม่ใช่ปัญหา **ทั้งสองอย่างแก้ได้ด้วยการวัดแยกก่อนลงมือ**

**สิ่งที่ยังไม่ได้ทำ**

- ยังไม่ปิดรายการ "Aetox ตอนทำงานจริง" — รอบนี้ไม่ได้จดสภาพ (กี่เทิร์น กี่แท็บ เปิดมานานเท่าไร) จึงทำซ้ำไม่ได้
- ยังไม่ได้วัดว่า 196 MB ของ gpu-process มาจาก layer ไหนบ้าง ต้องเปิด `--remote-debugging-port` แล้วดู layer panel
- `bench.ps1` ยังไม่มีโหมดแยกรายโปรเซส — รอบนี้ใช้สคริปต์นอก ซึ่งแปลว่ายังทำซ้ำอัตโนมัติไม่ได้ และควรย้ายเข้ามา

### 2026-09-08 — ทดลอง PGO แล้วตัดสินใจไม่เอา

คำถามตั้งต้น: Go 1.27 มี Profile-Guided Optimization ที่โฆษณาว่าเร็วขึ้น 2–7% โดยไม่ต้องแก้โค้ด — คุ้มไหม
**ไม่คุ้ม** และไม่ได้ตัดสินจากความเห็น แต่จากสี่อย่างที่วัดได้

**1. แทบไม่มี CPU ให้ประหยัดตั้งแต่แรก**

`aetox.exe` เปิดมา 39.7 นาที ใช้ CPU สะสม **11.1 วินาที = เฉลี่ย 0.46% ของหนึ่งคอร์**
ช่วงสุ่มวัด 12 วิได้ **0.00%** — ที่ขยับคือ browser 0.39% กับ gpu-process 0.13% ซึ่งเป็น C++
ของ Chromium ไม่ใช่โค้ดที่ Go compiler มองเห็น

**2. หนึ่งในสามของ CPU ที่เหลือ PGO แตะไม่ได้**

โปรไฟล์จาก benchmark สามแพ็กเกจ (`model` · `runlang` · `skill`) merge เข้าด้วยกัน:

| ฟังก์ชัน | flat | |
|---|---|---|
| `runtime.cgocall` | **34.0%** | syscall ของ Windows — PGO ปรับไม่ได้เลย |
| `model.(*streamToolAccumulator).add` | 3.5% (cum 16.7%) | hot path จริงของการสตรีม |
| `regexp` (`add` + `step`) | cum 7.4% | |
| ที่เหลือ | | GC · lock · memmove |

**3. เทียบ before/after แล้ววัดไม่ได้ เพราะ noise กลบหมด**

รัน `go test -bench -count=6` สองรอบ (เปล่า · `-pgo=<โปรไฟล์ที่ merge แล้ว>`) ผลกระจายสองทาง
ตั้งแต่ **−30.3% ถึง +26.6%** ซึ่งอ่านไม่ได้เลย เพราะ **baseline ตัวเดียวกันเหวี่ยงเอง**:

| benchmark | baseline เหวี่ยงเอง (min→max ใน 6 รอบ) |
|---|---|
| StreamAccumulatorLargeWrite | **±107%** |
| RunnableOnThisMachine | ±39% |
| StreamAccumulatorLargeWriteContentFirst | ±27% |
| RunnableWithNothingInstalled | ±21% |
| GrepRealRepo | ±17% |
| SubjectFromPartialArgs | ±12% |
| ResolveSandboxRoot | ±5% |
| ResolveSandboxPath | ±3% |

สัญญาณที่ตามหา (2–7%) เล็กกว่า noise ของเครื่องเกือบทุกแถว — **การทดลองนี้ไม่มีกำลังพอจะตอบคำถาม**
ทั้งที่มันเข้าข้าง PGO เต็มที่อยู่แล้ว: ใช้ benchmark ชุดเดียวกันทั้งเก็บโปรไฟล์และวัดผล เท่ากับให้
คอมไพเลอร์เห็นข้อสอบก่อนสอบ **ถ้าสภาพที่เอื้อขนาดนี้ยังวัดไม่ออก สภาพจริงยิ่งไม่ต้องพูดถึง**

**4. ราคาที่ต้องจ่าย — วัดเป๊ะเพราะ deterministic ไม่มี noise**

| build | ขนาด | ต่าง |
|---|---|---|
| `go build ./cmd/aetox` | 35,266,560 bytes (33.63 MB) | — |
| `go build -pgo=... ./cmd/aetox` | 35,410,432 bytes (33.77 MB) | **+143,872 bytes (+0.41%)** |

บวกภาระที่ไม่มีตัวเลข: โปรไฟล์ต้องเก็บใหม่ทุกครั้งที่โค้ดเปลี่ยน ไม่งั้นมันชี้คอมไพเลอร์ไปทางที่ตายแล้ว

**ผลข้างเคียงที่เกือบกลายเป็นตัวเลขปลอมบนเอกสารนี้**

ระหว่างทางวัดได้ว่า `-ldflags="-s -w"` ลดไบนารีลง **9,016,320 bytes (−25.6%)** ซึ่งดูเป็นกำไรก้อนโต
**แต่ไม่ใช่** — `go version -m` ของไบนารีที่ปล่อยจริงบอกว่ามันถูก build ด้วย
`-ldflags="-w -s -H windowsgui"` อยู่แล้ว เพราะ `wails build` ใส่ให้เองในโหมด production
กำไร 25.6% นั้นมีอยู่แค่ในไบนารี dev ของ [build.ps1](build.ps1) ซึ่งเป็นที่ที่**ต้องการ** symbol ไว้ดีบัก

> ข้อ 5 อีกรอบ — ถ้าไม่ได้เปิด `go version -m` ดู ตัวเลข "ลดขนาดได้ 8.6 MB" ก็ขึ้นเอกสารไปแล้ว

**สรุป: ไม่เอา PGO** — ไม่มีการแก้โค้ดใด ๆ ในโปรเจกต์จากการทดลองนี้ ไฟล์ทดลองอยู่นอก repo ทั้งหมด

**บทเรียนที่ใช้ต่อได้ — กติกาข้อ 4 ไม่พอถ้าไม่ทำข้อ 2**

ข้อ 4 บอกให้รัน 5 รอบแล้วเอา median รอบนี้รัน 6 รอบแล้วยังเจอ ±107% เพราะไม่ได้ทำตามข้อ 2
ตอนวัดมี claude 8.4 GB กับ Antigravity 2.6 GB เปิดอยู่ **ข้อ 2 ไม่ใช่ของประดับ มันคือเงื่อนไข
ที่ทำให้ข้อ 4 ใช้ได้จริง** — ตัวเลขความเร็วใด ๆ ที่วัดโดยไม่ปิดของอื่นก่อน อ่านได้แค่ว่า "ไม่ต่างกันเกิน ±100%"

### 2026-08-25 — วัดขนาด v1.5.7

โหลด `aetox-windows-amd64-portable.zip` จากหน้า release แตกออกมา อ่านขนาด `aetox.exe`
ไฟล์เดียวข้างใน = **50,818,560 bytes = 48.5 MB** · zip 20,913,488 bytes = 19.9 MB ·
ตัวติดตั้ง 22,292,297 bytes = 21.3 MB

ตัวคูณในตารางข้อ 4 ยังหารด้วย 33 MB ของ 27 ก.ค. อยู่ ทั้งที่หัวข้อ 7 ส.ค. ถอนตัวเลขนั้นไปแล้ว
แก้ตามของจริงแล้ว: Claude Code 7 → **5** · OpenCode 15 → **10** · Cursor 26 → **18** ·
VS Code 35 → **24** · Ollama 85 → **58**

### 2026-08-07 — 33 MB ไม่จริงแล้ว วัดใหม่ได้ 40.8 MB

README กับหน้าเว็บเขียน 35 MB ส่วนเอกสารนี้เขียน 33 MB — สองที่ที่ตอบคำถามเดียวกันแล้วไม่ตรงกัน
และ**ไม่มีอันไหนถูก** ตัวเลข 33 MB วัดไว้ 27 ก.ค. ตอนมี tools 20 ตัว วันนี้มี 48 ตัว
บวก goja ที่ฝังมาเป็นอินเทอร์พรีเตอร์ทั้งตัว binary จึงโตขึ้นตามที่ควรจะโต

**วิธีวัดรอบนี้ (ใครก็ทำซ้ำได้ในหนึ่งนาที):** โหลด `aetox-windows-amd64-portable.zip` ของ
v0.9.2 จากหน้า release แตกออกมา ข้างในคือ `aetox.exe` ไฟล์เดียวจบ = **42,822,656 bytes =
40.8 MB** · ตัว zip เอง 14.2 MB · ตัวติดตั้ง 12.6 MB
**ตัวติดตั้งจัดการ Tesseract/poppler/ffmpeg ให้ด้วย — พวกนั้นเป็นโปรแกรมแยก ไม่นับรวมในนี้**

ตัวคูณที่ต้องแก้ตามไปด้วย: เล็กกว่า Claude Code **5.8 เท่า** (เดิมเขียน 7) · Cursor **21 เท่า**
(เดิมเขียน 25–26) · VS Code **29 เท่า** (เดิมเขียน 35) — ยังเป็นตัวเลขที่ดี และตอนนี้เป็นตัวเลขที่จริง

> นี่คือกรณีตัวอย่างของข้อ 5 ที่เขียนไว้เองว่า **"ตัวที่เข้าข้างเราคือตัวที่อันตรายที่สุด เพราะไม่มีใคร
> อยากตรวจตัวเลขที่ทำให้ตัวเองดูดี"** — 33 MB อยู่บนหน้าเว็บมาสามสัปดาห์หลังจากที่มันเลิกจริงไปแล้ว
> เพราะไม่มีอะไรบังคับให้วัดซ้ำ ขนาดของ artifact ที่ปล่อยจริงควรถูกวัดทุกรุ่นตอนออกรุ่น
