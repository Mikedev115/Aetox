# AETOX — มาตรฐานการออกแบบหน้าจอ

> เอกสารนี้คือ**กฎที่ต้องยึดตอนกำลังจะทำจอใหม่** ไม่ใช่บันทึกว่าเคยตัดสินใจอะไร
> (นั่นคือ [docs/DECISIONS.md](docs/DECISIONS.md)) และไม่ใช่คำอธิบายว่าระบบทำงานยังไง
> (นั่นคือ [ARCHITECTURE.md](ARCHITECTURE.md))
> ถ้าสามไฟล์นี้ขัดกันเรื่อง**หน้าตาและการโต้ตอบ** ไฟล์นี้ชนะ
> ทุกข้อมาจากจอจริงที่พังมาแล้ว และอ้างอิงกลับไปที่ § ที่บันทึกเหตุผลไว้

ตกลงกัน 17 สิงหาคม 2026 หลังรื้อจอเปิดโปรแกรมครั้งแรกห้ารอบ · ที่มา §124

---

## 1. พื้นที่และการจัดวาง

**เนื้อหาอยู่กลางทั้งแนวตั้งและแนวนอน ความว่างที่เหลือต้องเท่ากันซ้ายขวา**
ความว่างไม่ใช่ศัตรู ความว่างที่ไม่สมมาตรต่างหาก จอต่อโมเดลเคยตรึงคอลัมน์ไว้ชิดซ้ายบนหน้าต่าง
1900px แล้วปล่อยขวาตายไป 900px และตารางสี่คอลัมน์ที่มีการ์ดสองใบทำให้การ์ดไปกองซ้าย
ทั้งสองครั้งเจ้าของเห็นก่อนเทสต์ · วัดได้ ไม่ใช่รสนิยม: ระยะซ้ายต้องเท่าระยะขวา

**หนึ่งจอ หนึ่งคำถาม** ทางเลือกรองลดเป็นลิงก์เล็กใต้ของหลัก ไม่ใช่กางออกมาพร้อมกัน
สามกลุ่มบนจอเดียวคือตาราง ไม่ใช่คำถาม

**ตัวเลือกเกินเจ็ด ต้องมีคำถามคั่นก่อน**
รายชื่อผู้ให้บริการ 17 เจ้าเรียงตามตัวอักษร บังคับให้ผู้ใช้แปลจาก "ผมจ่าย ChatGPT รายเดือน"
เป็น "แถวไหนในลิสต์" ก่อนถึงจะขยับได้ ถามด้วยภาษาของสิ่งที่ผู้ใช้ถืออยู่ แล้วค่อยกางเฉพาะหมวดที่ตอบ

**ยืมโครงได้ ห้ามยืมเครื่องประดับ**
จัดกลาง ทีละคำถาม ลิงก์เล็ก เป็นโครงที่ยืมจากใครก็ได้ แต่ของตกแต่งต้องเป็นของ Aetox
และถ้าแอปมีของแบบนั้นอยู่แล้ว ให้ใช้ตัวเดิมด้วย selector เดียวกัน อย่าสร้างของใหม่ที่คล้ายกัน
(ลายน้ำจอติดตั้งกับหน้าแชทเปล่าใช้กฎ CSS เดียวกันจริงๆ ไม่ใช่ค่าที่ก๊อปมาวาง)

**ห้องยืมของห้อง ทะเบียนยืมของทะเบียน** ห้อง (ทีมงาน โปรเจกต์ ความสามารถ) คือของที่ใช้
ทะเบียนในตั้งค่าคือฟอร์ม ห้องวาดด้วย `.sec-head` `.ag-band` `.office-grid` `.chair-card.agc`
ไม่ใช่ `.set-row` ของทะเบียน และหนึ่งห้องถือของชนิดเดียว ไม่มีแท็บเลือกชนิด · ที่มา §253

**ของชิ้นเดียวมีที่แก้ที่เดียว** ลิสต์ `for:` ของเซิร์ฟเวอร์เคยแก้ได้สามที่ เพราะมีสองคำถาม
(ตัวนี้ใครใช้ · คนนี้มีอะไร) แล้วแต่ละที่ตอบคำถามเดียว ถ้าพบว่ามีสองที่ ให้ยุบ ไม่ใช่ทำให้ตรงกัน
และหน้าที่เหลือแค่ชี้ไปที่อื่นต้องถอดออกจากเมนู (ข้อ 3 หมวดที่ว่างเปล่าห้ามวาด) · ที่มา §253

## 2. คำ

**คำที่ไม่จริงคือบั๊ค ระดับเดียวกับโปรแกรมพัง**

**ค่าบูลีนที่เป็นจริงได้จากหลายเหตุผล เอามาเป็นป้ายไม่ได้**
`HasAPIKey` เป็นจริงเมื่อ *มีคีย์* หรือ *ไม่ต้องใช้คีย์* หรือ *ลงชื่อเข้าใช้แล้ว* — สามเรื่องคนละโลก
พอเอามาแปะป้ายเดียวจึงได้คำว่า "มีคีย์แล้ว" ห้อยท้ายแม้แต่ผู้ให้บริการที่ผู้ใช้ไม่เคยติดตั้ง
ก่อนเอาค่ามาแสดง ให้ถามว่ามันเป็นจริงได้จากกี่ทาง ถ้าเกินหนึ่ง ต้องแตกเป็นหลายคำถามก่อน
(ที่ถูกคือสามคำถาม: ต้องมีเครดิตไหม · รับคีย์ที่วางมาไหม · ตอนนี้มีหรือยัง)

**ยาวกว่าแต่ชัดกว่า ชนะสั้นแต่คลุมเครือ** และให้มีคำเด็ดขาดอยู่ในนั้น
"ใช้ API คีย์ของผู้ให้บริการอื่นๆ" ไม่ใช่ "มี API key" · "รันโมเดลภายในเครื่อง Local 100%"
ไม่ใช่ "รันในเครื่อง"

**ห้ามใช้ em dash ในสตริงที่ผู้ใช้เห็น** (มติเดิม ดู [docs/DECISIONS.md](docs/DECISIONS.md))

## 3. ท่าทีของผลิตภัณฑ์

**ไม่มีทางออกที่ทำให้ผู้ใช้ค้างครึ่งทาง** ถ้าจอนั้นขอให้ตัดสินใจ ก็ให้ตัดสินใจจริง
ไม่ใช่แขวนไว้แล้วไปเจอปัญหาทีหลัง

**แต่ทุกครั้งที่เอาปุ่มข้ามออก ต้องระบุให้ได้ว่าประตูไหนคือประตูที่เปิดได้เสมอ**
ถ้าตอบไม่ได้ แปลว่ายังเอาออกไม่ได้ (จอติดตั้งตอนนี้ตอบว่า "ลงโปรแกรมโมเดลในเครื่อง"
ซึ่งยังทำได้เสมอ — วันที่คำตอบนี้ไม่จริง กฎข้อบนต้องถูกทบทวน)

**หมวดที่ว่างเปล่าห้ามวาด** ตัวเลือกที่ไม่มีอะไรอยู่ข้างหลังไม่ใช่ตัวเลือก
มันคือทางตันที่ใส่เสื้อผ้าของตัวเลือก

**จอที่คนทำมองไม่เห็น ต้องมีสวิตช์ให้มองเห็น**
วิซาร์ดมีทางลัด "มีคีย์แล้วถือว่าผ่าน" ซึ่งเป็นสภาพของทุกเครื่องที่พัฒนามัน มันจึงเน่าโดยไม่มีใครเห็น
สถานะไหนที่เครื่องนักพัฒนาไปถึงไม่ได้เอง (ฐานข้อมูลว่าง โทเคนหมดอายุ ไม่มีสิทธิ์ โควตาหมด)
สถานะนั้นต้องมีปุ่มพาไป และปุ่มต้องชนะทางลัดทุกอัน — ล้าง flag เฉยๆ ไม่นับ

## 4. การเคลื่อนไหว

**อนิเมชั่นตอบคำถามสองข้อ ไม่มีข้ออื่น: กดติดไหม และตอนนี้มันทำอะไรอยู่**
อะไรที่ไม่ตอบสองข้อนี้คือของเกิน ไม่มีอนิเมชั่นเปิดตัวช้าๆ เพื่อความสวยในแอปนี้

**ตัวเลขอยู่ที่ `styles/type.css` ที่เดียว เอกสารนี้เรียกชื่อโทเคน ไม่เขียนมิลลิวินาที**
เขียนตัวเลขลงเอกสารเมื่อไหร่ มันจะกลายเป็นที่ที่สองที่ตอบคำถามเดียวกัน แล้ววันหนึ่งจะไม่ตรงกัน

| จังหวะ | โทเคน | ใช้กับ |
|---|---|---|
| กด | `--dur-press` | ยุบลง 1.5–4% ยิ่งของเล็กยิ่งยุบเยอะ |
| เมาส์มาถึง | `--dur-tint` | สีตอบมือ: hover, focus, เปิด/ปิด — และเมนูลอยที่งอกออกจากขอบที่มันเกาะ |
| ความยาวเปลี่ยน | `--dur-glide` | แผงกว้างขึ้น แถบเติม สิ่งที่ยืดหดโดยไม่ใช่การมาถึงของทั้งจอ |
| จอมาถึง | `--dur-arrive` + `--ease-arrive` | จางเข้าพร้อมเลื่อนขึ้นเล็กน้อย |
| เลือกได้แล้ว | `--dur-settle` + `--ease-overshoot` | ติ๊กถูกเด้ง เฉพาะสิ่งที่ผู้ใช้เป็นคนทำให้เกิด |
| กำลังทำงาน | ตามงานจริง | วงหมุน **พร้อมคำที่ระบุชื่อสิ่งที่ทำ** ไม่ใช่ "กำลังโหลด…" ลอยๆ |
| ทำเสร็จ | `--dur-hold-success` | บอกว่าเสร็จ ค้างให้อ่าน แล้วเลื่อนต่อเอง ไม่ให้กดซ้ำ |
| ปิดตัวเอง | `--dur-hold-done` | จอสุดท้ายยืนนิ่งก่อนหายไป |
| ฉากหลัง | `--dur-ground` | ลายน้ำหลังห้องว่าง |

จังหวะที่สคริปต์เป็นเจ้าของ (setTimeout) ให้อ่านโทเคนผ่าน `lib/motion.ts` ไม่ใช่พิมพ์ตัวเลขซ้ำ

**ห้ามมีสถานะไหนที่รู้ได้จากการเคลื่อนไหวอย่างเดียว**
ทุกสถานะต้องต่างกันด้วยสีและด้วยคำด้วย เครื่องที่ตั้ง `prefers-reduced-motion` ปิดอนิเมชั่นทั้งหมด
แล้วทุกอย่างต้องยังอ่านออกเหมือนเดิม

**คำตอบของการกดอยู่ที่แถวที่ถูกกด** ไม่ใช่วงหมุน 13px ที่มุมอื่นของจอ
และงานที่จบต้องพูดว่าจบ ไม่ใช่หายไปเฉยๆ

## 5. ค่าที่แสดง

**ถ้า UI ต้องโชว์ค่าที่ระบบอื่นเป็นเจ้าของ ให้ไปอ่าน อย่าเขียนใหม่**
สีในตารางเลือกธีมอ่านจาก `styles/theme.css` ตอนรัน (`lib/themeSwatches.ts`) ไม่มีตารางสีชุดที่สอง
ลิงก์ "ขอคีย์ที่นี่" ถามจากทะเบียนผู้ให้บริการฝั่ง Go ไม่ใช่ตาราง URL ใน TypeScript
หลักเดียวกับ [docs/DECISIONS.md](docs/DECISIONS.md) เรื่องหนี้ในระบบ: ที่ที่สองที่ตอบคำถามเดียวกันคือหนี้

## 6. วิธีทำงานกับเอกสารนี้

**เทสต์ต้องล็อกสิ่งที่ห้ามมี ไม่ใช่แค่สิ่งที่ต้องมี**
จอแรกมีปุ่มได้แค่สองปุ่ม · ลิงก์ในจอต่อโมเดลต้องเท่ากับ "ย้อนกลับ" อย่างเดียว ·
หมวดลงชื่อเข้าใช้ต้องไม่มีช่องรหัสผ่านโผล่แม้แต่ครั้งเดียว ข้อห้ามคือสิ่งที่พังเงียบที่สุดตอนมีคนมาแก้ทีหลัง

**เปลี่ยน UI ต้องเปิดของจริงดู และต้องวัด**
สามข้อบกพร่องของรอบที่เขียนเอกสารนี้ เทสต์จับไม่ได้เลยสักข้อ (ป้ายพูดซ้ำ · การ์ดไม่อยู่กลาง ·
ช่องว่างครึ่งจอ) ทั้งหมดเห็นตอนเปิดแอปจริง และยืนยันด้วยตัวเลขที่วัดได้ ไม่ใช่ "ดูเหมือนจะดีขึ้น"

**วัดแล้วไม่จริง ให้ถอย** อย่าทิ้งคอมเมนต์ที่อ้างว่าแก้อะไรที่มันไม่ได้แก้
โดยเฉพาะในโค้ดที่ใช้ร่วมกันหลายที่ คอมเมนต์ที่โกหกอันตรายกว่าไม่มีคอมเมนต์

**เสนอเป็นของที่เห็นและกดได้ วางเทียบกัน มีชื่อให้ชี้** ไม่ใช่บรรยายทางเลือกเป็นย่อหน้า
