# desktop/ — the screen

> Module map: [ARCHITECTURE.md §4.2](../ARCHITECTURE.md) · The engine: [internal/engine/README.md](../internal/engine/README.md) · Direction: [remote-engine](../docs/architecture/remote-engine-2026-09-11.md) (§248) · Deep dives: [desktop-app](../docs/architecture/desktop-app-2026-07-22.md) · [browser-security](../docs/architecture/browser-security-2026-07-21.md) · [computer-use](../docs/architecture/computer-use-2026-09-07.md) · Tests: `TEST-REPORT.md` Module 5

**What it is:** the Wails v2 + Svelte 5 window, and since §248 B1 (2026-09-12)
*only* the window. `App` is what Wails binds and the frontend calls; every
binding that is engine work forwards to `internal/engine` through one
interface ([engine_forwarders_gen.go](engine_forwarders_gen.go), generated),
and every binding that needs a window — a WebView2 tab, the mouse, a dialog,
the file manager, a synthesizer, a credential — is written here. The engine
is a process beside this one, `cmd/aetox-engine`, started and supervised by
[engine_local.go](engine_local.go) and reached over one socket: `App.api` is
the `*rpc.Client` on it, and nothing above it knows (§248 phase 2, the same
day). There is no in-process engine any more — a test's engine is behind a
loopback listener too (`newTestApp(t)`).

The engine sees this window as `engine.Screen` ([screen.go](screen.go)),
implemented by `appScreen` — an adapter rather than `App` itself, because
every exported method of `App` is a binding.

**Desks (ARCHITECTURE.md §83/§84, product picture in [COMPANY.md](../COMPANY.md)):** a session is opened at a desk and stays there — the engine's business now (`internal/engine/office.go`, `workbench_desk.go`); the five buttons are the frontend's ([lib/desks.ts](frontend/src/lib/desks.ts)).

## Dev commands

- `wails dev` (or [wails-dev.bat](wails-dev.bat)) — live dev, frontend hot reload; browser dev server at `http://localhost:34115`. The `.bat` also sets `AETOX_DATA_ROOT` so repeated dev runs don't grow Aetox's own data (preferences, sessions, WebView2 profiles, the downloaded rtk binary) in `%AppData%` without bound — see `internal/config.DataRoot` and [ARCHITECTURE.md §14](../ARCHITECTURE.md). Production builds don't set this env var, so shipped behavior is the normal `%AppData%\aetox` default.
- `wails build` — production `desktop.exe`.
- `go run ./internal/engine/gen -root .` after any exported-method change on either side — regenerates `internal/engine/api_gen.go` and [engine_forwarders_gen.go](engine_forwarders_gen.go); `gen_test.go` fails the build when they are stale. Then `wails generate module` from this folder for `frontend/wailsjs/` — Wails groups TS types by Go package, so a type that moves between `engine` and `main` moves namespace.
- `go test ./desktop/ ./internal/engine/ ./internal/engine/rpc/ ./cmd/aetox-engine/` + `npx svelte-check` in `frontend/` — run them before calling a change done. The desktop suite runs every test over the wire; `engine_local_test.go` builds the real engine binary and drives it as a child.
- Go's own `GOCACHE`/`GOMODCACHE` are machine-wide settings, not project config — if they're growing on `C:`, `go env -w GOCACHE=<path>` / `GOMODCACHE=<path>` moves them anywhere, for every Go project on the machine, not just this one.

## Go side (package main)

| File | Role |
|---|---|
| [app.go](app.go) | `App`: `api engine.API` (the `*rpc.Client`), `client` (the same, for the pieces outside the bindings), `engine` (the child and the wire, engine_local.go), `ctx` (the window's lifetime — dialogs, sizing, Quit), the test seams `emit`/`openDir`, and the state that is the screen's own: the browser host, the machine lock, the read-aloud jobs, what the deck export wrote. `NewApp` builds the client with the whole Screen served on it (`rpc.ServeScreen`); `startup` starts the child; `beforeClose` → `PrepareToClose` across the wire; `shutdown` tells the child to leave and waits, then `stopAllSpeech`. |
| [engine_remote.go](engine_remote.go) · [screen_config.go](screen_config.go) | The engine on a host over ssh (§248 phase 3): `spawnRemote` walks `remote.Driver.Connect` with each step on the status chip and hands the supervisor the tunnel as its process; `engineBinaryFor` finds this version's Linux engine (`AETOX_ENGINE_LINUX_DIR`, beside the exe, else `update.FetchEngine` from the release); `screen.json` (atrest) is the hosts and their tokens; the bindings behind Settings › เครื่องระยะไกล (`RemoteHosts`, `SaveRemoteHost`, `ForgetRemoteHost`, `ConnectRemote`, `DisconnectRemote`, `StopRemoteEngine`, `RemoteEngineLog`) are the screen's own, so they answer while the engine is unreachable. A switch of engine reloads the frontend. |
| [host_files.go](host_files.go) | The doors with the engine on a host (§248 phase 4). A file a dialog here picked goes up the wire first (`rpc.UploadFile` → the engine's inbox) and the binding is handed the path THERE — `SaveChatImage`, `SaveChatFile`, `AddSpaceContextFiles` are the screen's for that reason, and screen_doors.go's import/preset/skill-zip doors go through `withHostFile`. A folder is picked THERE: `pickHostDir` raises `screen:pickdir`, the window shows `RemoteDirPicker` with the dialog's title, `AnswerHostDir` brings the pick back. A reveal on a host is a named error with the host and the path; "open with its program" fetches a copy through `/file/` and opens that. At home every one of these is the path as it is. `host_files_smoke_test.go` runs the same doors against a real host (`AETOX_REMOTE_SMOKE`). |
| [engine_local.go](engine_local.go) | The engine as this window's child (§248 phase 2) — and the supervisor for whichever engine the target names, a child, an attached one (`AETOX_ENGINE_ADDR`) or a tunnel (engine_remote.go): `aetox-engine.exe` beside the app (`AETOX_ENGINE` overrides; a development tree falls back to `go run ./cmd/aetox-engine`), a token on its stdin, a unix socket under DataRoot or a loopback port, `hello` with the window's tools. The supervisor redials a dropped wire, restarts a dead child with backoff, gives up after three in a minute until `RestartEngine`; `EngineStatus` and the `engine:status` event feed [EngineStatus.svelte](frontend/src/lib/EngineStatus.svelte), and the frontend reopens the chat on screen after a restart (`resyncAfterEngineRestart`). |
| [screen.go](screen.go) | `appScreen` — `engine.Screen` for this window: `Emit`, `ProviderTransport` (provider_forward.go), `ProviderEndpoint`, `WindowTools` (the browser and the machine, per session), `AgentTab`, and the three desk questions answered with this machine's key: `DefaultModel`, `Probe`, `ModelResident`. `screen_test.go` — a fake Screen proves the engine asks nothing else. |
| [provider_forward.go](provider_forward.go) | The screen's half of the model credential (§248 A3): a `model.Transport` that signs each provider request from `credentials.json`/`oauth.json` at the moment it goes out. The engine never holds a key and `config.Config` has no field for one (A4); `internal/engine/deps_test.go` holds that line. |
| [providers.go](providers.go) · [oauth.go](oauth.go) · [account.go](account.go) | The credential desk: `SetAPIKey`, `HasAPIKey`/`APIKeyHint`, custom OpenAI-compatible rows (each with its own key), `TestProviderConnection` and the model discovery that needs a key, provider sign-in (`StartSignIn`/`CompleteSignIn`/`ImportSignIn`/`SignOut`), the Aetox account. A change is reported to the engine as `ProviderCredentialChanged`, which rebuilds only if it is the provider on screen. |
| [screen_doors.go](screen_doors.go) | Every native dialog and every "show it in the file manager" button, in one file (§248 A5). Each is the *screen's* half of a pair: it asks or shows on this machine, then calls the engine twin that takes a path, hands back bytes, or answers with a host path. `screen_doors_test.go` pins that every door has its twin, by reflection. [exports.go](exports.go) is the one door with no twin: a deck export lands in *this* machine's Downloads (`engine.DeckExportFiles` gives the bytes), and `OpenExport` opens only what this session wrote. |
| [browser.go](browser.go) and `browser_*.go` | Native WebView2 tab host via raw Win32 syscalls — Windows-only. Read [browser-security](../docs/architecture/browser-security-2026-07-21.md) **before** touching `onMessage`/`metaScript`/`textScript`. `CloseAllBrowserTabs` (called once by the frontend on load) sweeps tabs orphaned by a previous frontend lifetime and spares a running turn's (`engine.AnyTurnRunning`). The picture the browser takes goes to the engine (`SaveBrowserShot`), which decides where in the project it lives. |
| [workbench.go](workbench.go) · [browser_tool.go](browser_tool.go) | The one `browser` tool the model sees, twelve actions inside it, gated per action on the old `browser_*` names so `tools:` and `categories:` still narrow — and narrowed by stance through `Narrow`, which the engine calls. `open` resolves its address through the engine (`ResolveWorkbenchURL`) and writes a sentence the engine's `ParseBrowserOpened` reads back for the start page. |
| `computer_*.go` · [uia_windows.go](uia_windows.go) | Driving programs on this machine ([computer-use](../docs/architecture/computer-use-2026-09-07.md)): the guard and its tier table ([computer_guard.go](computer_guard.go)), the grant list the user can take back ([computer_permission.go](computer_permission.go)), UI Automation. The switch is the engine's to honour (`workbenchSkills` drops the tool when off); `SetComputerControlOn` tells it (`ComputerControlChanged`). |
| [deck_render.go](deck_render.go) · [deck_render_screen.go](deck_render_screen.go) · `deck_*.go` | A deck drawn in a hidden WebView2 parked off-screen — PDF, one PNG per slide, the flatten check, the reveal — answered to the engine as bytes through `Screen.RenderDeck`. A deck is a file on the engine's host; what it looks like is a question only a browser here can answer. |
| [speak.go](speak.go) · [voice.go](voice.go) · [ttshost.go](ttshost.go) | The reply's ฟัง button as a stream (pieces synthesized a few ahead of the listener, served at `/aetox-tts/`), the voice picker over this machine's installed voices, `SpeakText` for the preview. The pick itself is the engine's to remember (`VoiceSettings`/`RememberTTSVoice`); the mic and the STT pickers are the engine's outright. |
| [update.go](update.go) · [update_notify.go](update_notify.go) | Self-update (ARCHITECTURE.md §107): the daily check emitting `update:available`, `StageUpdate` (download + verify + swap), `RestartToUpdate` (gated on the engine's `ReadyToRestart`), `StagedUpdate`. |
| [attach.go](attach.go) · [window*.go](window.go) · [events.go](events.go) | Picking attachments and saving a drawing; window sizing; `emitEvent` — every event the screen sends, and the door `Screen.Emit` relays the engine's through. |
| [main.go](main.go) | Wails bootstrap. |

## Frontend (`frontend/src/`)

- **The five buttons** ([lib/desks.ts](frontend/src/lib/desks.ts)) — the nav row at the top of the sidebar. Two desks open a session (`openDesk` → `NewSessionAt`, and re-clicking the desk you are already at is a no-op on the conversation), two are full-window pages, ทำงานอัตโนมัติ is present and disabled. The pages ([lib/Office.svelte](frontend/src/lib/Office.svelte), [lib/Artifacts.svelte](frontend/src/lib/Artifacts.svelte)) render through the same `.settings-overlay` seam Settings has always used, so the chat layout underneath is untouched — the whole surface is additive. The list is fixed rather than built from `ListModes`: a fourth mode file is a fourth desk in the engine, not a fourth button in the product.
- [App.svelte](frontend/src/App.svelte) — layout, panel resize/collapse, Wails event subscriptions (`agent:status`, `agent:tool`, `agent:chunk`, `agent:reasoning`), file drop, `CloseAllBrowserTabs()` on mount.
- `lib/stores/cockpit.svelte.ts` — the single reactive state tree (chat, tool timeline steps, model info); seeds `cockpit.model` from a `localStorage` cache synchronously on load so first paint shows real dropdowns instead of an empty/loading state, then corrects it once the real `GetModelInfo()` call resolves. `lib/stores/workbench.svelte.ts` — workbench tabs.
- `lib/Chat.svelte` — chat, live tool timeline, live reply + reasoning streaming (unbounded height; finished messages keep their thinking collapsed under a "คิดเป็นเวลา Xs" toggle), pinned auto-scroll (follows the stream at the bottom, unpins when the user scrolls up), ask_user option cards (composer doubles as free-text answer), todo checklist panel, per-message copy button, and the ■ Stop button (send button morphs while a turn runs). A `file`/`browser` workbench tab can be dragged into the composer to inline its content as context (`attachTabContext`).
- `lib/workbench/` — Review/Files/Browser panes; drag source for the context-attach feature above (`Workbench.svelte`'s tab strip, `draggable` on file/browser tabs).
- `lib/selfUpdate.svelte.ts` + [lib/Updater.svelte](frontend/src/lib/Updater.svelte) — the update the user meets: one bottom-right card through four phases (offer → downloading, in MB → ready → error), never a modal, because nothing it reports interrupts the app. "ไว้ก่อน" on a ready update costs nothing — the build is already on disk and closing the app normally collects it. The store is the single owner of that state; Settings → About drives and reads the same one (ARCHITECTURE.md §107). Named `selfUpdate`, not `updater`: on Windows's case-insensitive filesystem `./updater.svelte` resolves to `Updater.svelte`, the component, and the import silently yields the wrong module.
- `lib/locales/{th,en}.ts` — i18n (add keys to **both**).
- Generated Wails bindings live in `frontend/wailsjs/` — `wails generate module` here after a Go change; `wails dev` regenerates them too. Types are namespaced by Go package: `engine.*` for the engine's, `main.*` for the screen's own (`StagedInfo`, `DeviceProfile`, `ComputerAppRow`, `AccountState`, `TTSVoiceInfo`).

## Rules of thumb

- Engine behavior does not belong here — if it isn't a window, a credential, or this machine's hardware, it goes in `internal/engine` (or `internal/*` below it) and the screen gets a forwarder for free.
- A binding here that needs the engine calls `a.api`, never `a.eng`: the interface is what phase 2 swaps for a socket, and what a test hands in with one answer changed (`engineWith`, `apptest_test.go`).
- `wailsruntime.EventsEmit` and every dialog need a real Wails context — code paths that tests reach go through `emitEvent`/`openDir`, which a test can seat; `a.ctx` nil is silence, not a crash.
- A binding that returns a slice returns it non-nil (ARCHITECTURE.md §34) — `computer_permission_test.go` holds `GrantedComputerApps` to it; the engine's `binding_slices_test.go` holds the rest.
